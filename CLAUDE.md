# Nexora — Claude Code entry point

@AGENTS.md

## Read order

`AGENTS.md` (imported above) is the operating contract. Then `PHASE_2_BRIEF.md` for current scope. `08_PHASE_1_BRIEF.md` is the closed phase's record — its §5 still explains the existing schema's RLS exemptions, but it is not current scope.

Do **not** load the whole documentation pack into one context window — `README_START_HERE.md` explains why. Pull sections of `03`, `04`, `05` and individual ADRs on demand. Never load `future/`, `99_SOURCE_MASTER_SPEC_v1.2.md`, or ADR-011 through ADR-018 during Phase 1 or 2.

When documents disagree: **ADR Index > Architecture RFC > Technical/Database/Contract docs > Platform Overview > Source Master Spec.**

## Current state

See `PROJECT_STATUS.md` for the full per-slice narrative. In short, as of 2026-08-28:

- Phase 0 and Phase 1 (all seven capabilities) are implemented and repaired. **The Phase 1 gate is OPEN** (`PHASE_1_GATE_OPEN_2026-08-27.md`). CI is live and green.
- A real production build exists (`npm run build`/`npm start`, proven booted under `npm ci --omit=dev`) and `.env` actually works (`cp .env.example .env` is sufficient, zero manual `export`). `npm run check:dist-deps` (in CI) fails the build if a devDependency ever leaks into `dist/` again.
- HTTP hardening: security headers, CORS denied by default, `X-Powered-By` disabled, `GET /health` (liveness + DB readiness, not a capability).
- `PHASE_1_DEBT_CLOSURE.md`: D-1, D-3, D-4, D-5, D-6 CLOSED (D-5 on the closing *decision*, not on either table being built); D-2 (Redis/BullMQ) PARTIALLY CLOSED.
- **Phase 2.5 (`Launch Readiness`, renamed from `Commercial Growth` on 2026-09-03) was created 2026-09-03** — subscription discounts (bulk and individual), referral codes, and ADR-020 rule 6's tenant data-export capability, in that order because a referral reward is paid *as a discount*. Enumerated in `06_IMPLEMENTATION_PLAN.md` (where phases live) and scoped in `PHASE_2_BRIEF.md` §9 (a dated amendment, the first that file has carried). **It defines scope and designs nothing.** One deadline reaches back: **the discount decision must land before Phase 2 item 13's migration**, because `invoices`/`invoice_lines` are append-only and migrations are forward-only, so adding a discount line later is a data migration. It explicitly excludes fraud scoring, marketplace, split payments, commerce coupons (those are Phase 3's merchant-facing coupons, a different subject) and the AI credit economy. **Widened the same day by ADR-054** to also carry the nightly snapshot job, operator-run restore, the recovery drill, and **object storage as their named prerequisite** — which the platform does not have and no phase owns (`R-025`, whose "two full phases away" rating is now superseded by a dated addendum). §9.8 records the unifying reading — *Phase 2 makes the product sellable, Phase 2.5 makes it responsible to run for real customers* — with the honest caveat that it fits the recovery half better than the discount half, and leaves open, as a maintainer decision, whether the name `Commercial Growth` should change or the phase should split.
- **Phase 2 is OPEN.** `PHASE_2_BRIEF.md` is `AGENTS.md` §1 authority #2 — **31 tables and 16 capabilities in scope as of its 2026-09-03 amendments** (27 + ADR-048's invoice-number counter and ADR-050's outbox delivery table, both owed by rulings that declined to amend the list themselves), 18 items, 26 exit criteria, and a §7 disposition table recording the fourteen decisions (D2-1 … D2-14) it was drafted around, all answered.
- **64 ADRs as of 2026-09-03** (56 ACCEPTED, **0 OPEN**, 8 DEFERRED — `PROJECT_GRAPH.md`'s ADR register is the mechanical count). **No ADR is open.** ADR-039 (pool sizing) and ADR-040 (observability) were the last two and were ruled on 2026-09-03, each following the recommendation already in its own text. **ADR-039 rules a derivation rather than a number** — `floor((max_connections − reserved) / (instances × pools_per_instance))`, with `pools_per_instance` **2 and not 1** because `connections.ts` creates `APP_DB` and `AUDIT_DB` per process — plus a startup refusal when oversubscribed, and four timeouts set server-side per role. Its tenant-scope half was **verified, not assumed**: `withTenantContext` passes `set_config(..., is_local => true)`, so the context is transaction-scoped and no connection returns to the pool carrying it. That property is now **guarded by `platform/db/tenant-context-pool-reuse.spec.ts`**, which proves connection reuse via `pg_backend_pid()` rather than hoping for it, and **was watched failing** when the argument was flipped. **ADR-040 rules the boundary before the library and adopts no dependency** — NestJS's `Logger` is already in use, and the ADR's own recommended option says the seam costs none; `pino` is named only as a future candidate. Logging is a port, **already enforced in `domain`** by the existing `^@nestjs/` pattern, and **owed for `application`**, which has no forbidden-package list at all. A log line must carry the `correlationId` `audit_events` records, so the two are joinable; the never-log list now includes **personal identifiers, because ADR-057 put کد ملی and a postal address into the platform on the same day**, and enforcement is stated honestly as a review rule rather than a checked one. **ADR-041 (ledger growth) was ruled `ACCEPTED` on 2026-09-03 with a fifth option its own text never listed: keep the append-only tables partition-*compatible* and do not partition them.** Its four PostgreSQL questions were answered empirically first, and the answers are why the ruling is not option 1 — partitioning weakens tenant isolation under this project's grants (`R-042`), permanently surrenders uniqueness on `id`, and cannot have a partition-creation job because `nexora_app` owns nothing. It binds the creating migrations of items 4, 5, 9 and 12 to three cheap negatives (an event column, no FK to a candidate table, no uniqueness excluding the event column) and is enforced by `npm run check:partitions`. **ADR-041's four PostgreSQL semantics questions were answered empirically on 2026-09-03** — transcripts are in its body — **and its ruling was deliberately withheld**, because the answers disqualified the option that was about to be adopted (see `R-042`). It stays `OPEN` with a named precondition. **New on 2026-09-03, all `ACCEPTED` and all ruled by the maintainer that day:** **ADR-051** was ruled (`ACCEPTED (was OPEN)`, Option B) — a membership revoked while a request from that session is in flight surfaces as `SESSION_INVALIDATED`/401, with the scope limit that a caller who merely lacks membership stays `403`; it implements nothing, and the guard changes are a later slice. **ADR-052** self-serve trial (`ACCEPTED (new)`) — **`plan.subscribe` starts the trial**, no payment and no separate capability, eligibility and duration on the **plan version** defaulting to 7 days, one trial per organization enforced by a database constraint; **its `Blocks` is Phase 2 items 1 and 2's creating migrations**, because those are plan-version columns and migrations are forward-only. Its abuse surface is `ACCEPTED` with a named reopening trigger, not claimed closed. **ADR-053** session retention (`ACCEPTED (new)`) — 30 days past unusable, one clock for revoked and expired, audited at the job-run level rather than per deleted row, and **`session.purge` is owed to Phase 2 item 12, which does not currently schedule it.** **ADR-054** per-tenant recovery (`ACCEPTED (new)`, ruled 2026-09-03) — R-038's restore half finally has a mechanism: **a nightly per-tenant logical snapshot, a 24-hour RPO, and arbitrary per-tenant point-in-time recovery explicitly declined on cost** (it would have needed WAL archiving, a standby restore target and a full-cluster restore per incident; a nightly snapshot needs none of the three). **RTO is deliberately unstated** and owed to the first drill, following ADR-010's precedent rather than inventing a number that would be quoted. **Snapshot and tenant export are one mechanism, not two.** **Ledger-shaped tables are never rewound** — an invoice issued after the snapshot point stays issued, and undoing it is a compensating entry — so restore runs outside the application role as an audited **operator action, never a tenant-facing capability**. Snapshots retain 30 days, tracking ADR-020 rule 3's *default* waiting period rather than a constant. **Two things it deliberately does not own: total cluster or server loss** (a different mechanism, covered nowhere — it recommends a risk row rather than opening one) and **object storage**, which is a hard prerequisite it names and `R-025` already tracks. **Amended the same day** to answer the one edge it had named: **`sessions` rows are excluded from the snapshot and never restored** — a restored session is a live credential that had been deliberately revoked between the snapshot and the incident, and a restore is exactly the circumstance in which one most likely was — **and a restore revokes the live sessions of that tenant's members**, reachable via `memberships.tenant_id → user_id → sessions.user_id` (verified against both creating migrations). The amendment also records the consequence the ruling implies but does not state: sessions belong to a **user**, not an organization, so this signs multi-org users out everywhere — accepted, because scoping by `active_organization_id` would miss a session that switched away and could switch back, and a live stale session is the worse failure. Stated plainly because it is the clearest available summary of where the project is: **the schema-shaping decisions are finished, and what remains open is operational — plus one contract question.** That one was **ADR-051**, ruled `ACCEPTED` on 2026-09-03 (Option B) and **implemented the same day** — the first production code this documentation programme produced. `SessionGuard` and `OrganizationAccessGuard` now raise `SESSION_INVALIDATED`/401 for a revoked session while a caller who merely lacks membership still gets `FORBIDDEN`/403, and all nine capabilities that use `SessionGuard` declare the code. It was opened as (`OPEN`, new 2026-09-02): which error code is correct when a membership is revoked while another request from that same session is in flight. It owns `R-036`, is the **last live thread of `R-008`**, and names `SESSION_INVALIDATED` — declared in `05` §7, mapped to 401, thrown nowhere — as the producer that would give `R-037` a path to closure. Its recommendation is Option B (normalize to 401 `SESSION_INVALIDATED`), advisory and unruled. It blocks nothing in Phase 2, and **the spec's status-code filter must not be widened before it is ruled** — that would ratify its Option A by omission. Seven ADRs were written on 2026-09-02 ahead of Phase 2 slice 1, and the four that were drafted `OPEN` were all ruled the same day; each carries a `### Ruling` section naming the maintainer and the date, with its `Problem`, options and `Recommendation` left intact as the record of what was considered. **ADR-044** localized display text (`ACCEPTED (was OPEN)` — Option A: the plan tables carry a machine `key` only and `plan.list` returns keys; the ruling's own safety condition is that **an invoice line must carry its own denormalized description**, an obligation on item 13); **ADR-045** optimistic concurrency (`ACCEPTED (was OPEN)` — a `version` column on `subscriptions`, `subscription_changes`, `billing_payment_intents` and `tenant_over_limit_states`; **`idempotency_records` excluded** because ADR-009's unique constraint already serialises its writers, and the six Tier 2 tables excluded with a named reopening trigger); **ADR-046** soft-delete (`ACCEPTED` — **no `deleted_at` column in Phase 2 and no Phase 2 query owes a soft-delete filter**; reopened by the first capability that deletes); **ADR-047** price version binding on renewal (`ACCEPTED` — **renewal re-pins to the current price version**, fixed at renewal-invoice issuance (T-30d); the number was reassigned from the strategic package's tenant-restore subject, which was not written); **ADR-048** invoice numbering (`ACCEPTED (was OPEN)` — **global, gap-free, allocated from a counter locked inside the issuing transaction; the integer is stored and the display form rendered**, and the Iranian tax unique number is a separate deferred field, its details labelled unverified against a primary source); **ADR-049** MCP readiness posture (`ACCEPTED` — builds nothing, names the three things in Phase 2 that would foreclose Phase 9's accepted MCP path); **ADR-050** financial event packet (`ACCEPTED (was OPEN)` — **delivery state lives in a separate table, no column-level exception to `outbox_events`' `REVOKE`**, and `event_version` ships from day one because it is the one envelope field that cannot be retrofitted). **ADR-055** tax on a subscription purchase (`ACCEPTED (new)`, ruled 2026-09-03) — **the published price is net and VAT is added at checkout**; `billing_payment_intents` persists the **gross** amount, because a net one makes every ADR-023 item 5 verification fail as a security event; `invoices` gains four `NOT NULL` tax columns **created with the table**, since it is append-only and rows already in it can never be filled in; and the rate lives in a dated append-only `tax_rates` table rather than a settings value, because Iran's VAT rate has actually changed and a mutable value cannot answer *what rate applied on the day this invoice was issued*. **ADR-022 was amended the same day** with items 9–13: `IRR` is the currency and Toman a display unit with divisor 10 — **most of which Phase 1's `currencies` table already implemented**, so the amendment confirms it rather than claiming it — plus two genuinely new rules, that **machine-readable output (`schema.org`, Open Graph, feeds, every API response) emits `IRR` and the Rial amount** because "Toman" is not an ISO 4217 code and a consumer reading it as Rials is silently wrong by ten, and that **a rendered invoice is denominated in Rials** while the UI shows Toman. **ADR-056** correction documents and **ADR-057** the buyer's legal identity (both `ACCEPTED (new)`, ruled 2026-09-03) apply one rule twice: `invoices` is append-only, so **a column can be added later but old rows can never be filled in** — therefore `document_type NOT NULL` (`INVOICE`/`CREDIT_NOTE`; Phase 2 issues only `INVOICE`) and the buyer's identity **snapshot columns** must exist at creation, while `corrects_invoice_id` may wait. A credit note draws from **the same** gap-free counter, stores **positive** amounts with the type carrying the sign, and snapshots **the rate of the invoice it corrects** rather than the rate of the day. ADR-057 adds a mutable `billing_profiles` table — **with no `version` column, because ADR-045's ruling is a named list of four tables and its Tier 2 logic excludes a table with one writer** — and **owed Phase 2 a capability**, which `PHASE_2_BRIEF.md` §3's amendment discharges as `billing.profile.set`, taking Phase 2 to sixteen. It removes one `R-043` blocker and closes none. **ADR-058** direct debit not in V1 (`ACCEPTED (new)`, 2026-09-03) — `supportsDirectDebit` stays in the port and stays **false**, enforced by ADR-023 item 1's existing startup check; the blocking reason is **ADR-037**, which guarantees the database never holds credential material but defers the *resolver*, so a mandate today would live in an environment variable — a fine trade for a gateway key that only *asks* for a payment, and not for an instrument that **pulls money from a bank account with nobody watching**. Four reopening conditions are recorded as a set. **ADR-023 and ADR-055 were both amended the same day** for the unit an Iranian gateway speaks: the port declares a **wire unit** (Rials or Tomans) and a **charging granularity**, the adapter is the only place conversion happens, verify compares in stored units, and an off-granularity amount at an adapter is a **hard failure**. The silent failure is the one this catches — converting outbound but not inbound compares equal and passes, tenfold — so the shared adapter contract suite gains a **unit round-trip assertion**. ADR-055 carries the arithmetic: `tax_amount_minor` is the value nearest nominal such that `subtotal + tax` lands on the granularity, which keeps the invoice identity exact and was **computed rather than asserted**. **ADR-059** what a resolving host returns when its store is not serving (`ACCEPTED (new)`, 2026-09-03) — **503 while the subscription can still be revived, 410 once the reactivation window closes**, computed by ADR-024 item 2's derived function rather than a second flag; never a `200` with an unavailable body (a soft 404 that keeps the placeholder indexed) and **never a redirect to a platform hostname**, which would move a tenant's traffic and search signals onto the platform's own domain. The ACME challenge path keeps answering; `robots.txt` deliberately does not. **Three ADRs were amended the same day.** **ADR-019** finally defines the bound that **three documents referenced and none defined** — ADR-019's own verification list, ADR-024's, and `06`'s Phase 4 Exit — as **two** bounds, 60 seconds at the origin and 5 minutes at the edge, with the line that makes it testable: **no storefront response may carry an `s-maxage` above 300**, because a purge is best-effort and the TTL is the only thing that holds when it fails. **ADR-028** rules that a **platform subdomain is never reissued to a different organization**, and narrows its own item 8 visibly to say so — *“free the hostname for a legitimate re-claim”* is right for a customer domain, where DNS control **is** the ownership check, and wrong for a platform subdomain, where nothing changed hands. **ADR-027** adds only what its item 3 did not already rule: the published address is an **edge** address, and the origin accepts traffic only from the edge. **One tension was surfaced and deliberately not ruled:** ADR-027 item 4 requires automatic renewal with no carve-out while item 10 says a certificate is *“left to expire naturally”* — if renewal stops, a suspended tenant gets a browser interstitial instead of ADR-059's clean 503. Recommended for settling; nothing is blocked, because ADR-059's rule holds under either reading. **ADR-060** object storage port and **ADR-061** cluster recovery (both `ACCEPTED (new)`, 2026-09-03) close the last two pre-Phase-2 gaps, and **neither builds anything**. ADR-060 is the contract only — S3 semantics chosen as **the intersection of the market rather than a supplier**, capability flags on ADR-023's discipline, and `supportsObjectImmutability` carrying the weight because *a backup a compromised credential can delete is not a backup*; `files` stays out of §4 and the adapter arrives with its first consumer. **ADR-061 states no targets of its own and cites ADR-010's** — `RPO 15 minutes`, `RTO 4 hours`, which that table has carried all along — because inventing a second, stricter RPO is how two numbers end up in circulation and the looser one gets quoted. **The finding is that the platform committed to 15 minutes long ago and its only recovery mechanism (ADR-054's nightly snapshot) delivers 24 hours for one tenant and nothing for the cluster**, while `01` and `03` already gate production on meeting it. Nightly base backup plus **continuous WAL archiving**, a copy outside the account running the cluster, and a quarterly drill; `RUNBOOK_DISASTER_RECOVERY.md` carries the procedure and opens by saying **nothing is provisioned**. **`R-041` stays OPEN until the first restore drill** — a backup is a hypothesis until a restore has been performed — and `R-025` stays open because a port shape is not an adapter. **ADR-027 was amended the same day** to settle the certificate question ADR-059 surfaced: **renewal continues while a subscription can still be revived and stops at `CANCELED`** — which **completes item 10 rather than overriding it**, since item 10's own purpose (*reactivation without re-issuance*) only works if the certificate is still valid through the window. That is **the same lifecycle boundary** ADR-059 uses for 503→410 and ADR-028's amendment uses to reserve a subdomain permanently. Its one contingency is recorded as a trigger: it costs nothing only while certificates come from an automated, free issuer. **Two tables are owed to `PHASE_2_BRIEF.md` §4's scope list** before items 13 and 14 respectively — ADR-048's invoice-number counter and ADR-050's delivery table. Amending that list is a scope decision belonging to that file, not to an ADR, and neither ADR discharged it.
- Superseded counts, kept for orientation: **53 ADRs as of 2026-09-02 earlier the same day** (38 ACCEPTED, 7 OPEN) — before the four rulings above; and **46 ADRs as of 2026-09-01** (35 ACCEPTED, 3 OPEN) — **ADR-041** ledger/audit table growth (`OPEN`, with the partitioning × `FORCE ROW LEVEL SECURITY` interaction recorded as *owed empirical verification* rather than asserted), **ADR-042** error message audience (`ACCEPTED`; **no localization work is owed in Phase 2**, as a ruling rather than a deferral — and ADR-044 above exists because that ruling covered error messages only, not content text), **ADR-043** guarding `CapabilityDefinition` against `05` §5 (`ACCEPTED`; asserts the *declared difference*). Before that, **43 ADRs as of 2026-08-28** (33 ACCEPTED, 2 OPEN, the rest deferred — `PROJECT_GRAPH.md`'s ADR register is the mechanical count). New on 2026-08-28: **ADR-036** pagination contract, **ADR-037** credential storage + encryption deferral, **ADR-038** idempotency composition (all `ACCEPTED (new)`, promoted out of `PHASE_2_BRIEF.md` §5 so a platform contract does not expire with the phase); **ADR-039** pool sizing/timeouts and **ADR-040** observability boundary are `OPEN` — options and a recommendation recorded, the ruling is the maintainer's. **ADR-006 and ADR-010 carry dated amendments** — ADR-006's `Blocks` was *split* (its usage-ledger half still blocks Phase 2 item 9; only the AI-credit half moves to Phase 6+), and ADR-010's numeric targets are now explicitly unverified assumptions until `06` Phase 4 item 9.
- Five items remain owed to *other* files (`PROVIDER_MATRIX.md`, `06`'s re-phrasing, an `entitlement.resolve` inner-path p95 budget, two undefined lifecycle cells, the role-catalog question); none blocks slice 1, and each is tracked as a register row.
- `TECHNOLOGY_RADAR.md` and `COMPETITIVE_POSITION.md` (both 2026-09-01) are **non-normative** — they inform decisions and make none, are deliberately absent from `AGENTS.md` §1's read order, and point at the ADR or register row that owns each decision rather than restating it. Their external claims are explicitly unverified against upstream sources.
- `RISK_REGISTER.md` roll-call, **44 rows as of 2026-09-04** (the date is part of the claim — an undated count reads as current forever): **nine rows are not OPEN** — **CLOSED** R-001, R-007, R-009, R-014 · **RESOLVED** R-002, **R-036**, R-040 · **ACCEPTED** R-003 · **PARTIALLY CLOSED** R-005. **The other 34 are OPEN.** **R-044** (new 2026-09-04) is a **spend** control rather than a notification feature: an SMS endpoint with no per-recipient interval and no per-tenant daily cap converts an unauthenticated request into an outbound payment, and the endpoint working as designed *is* the attack. It ships with Phase 2 item 17's first channel, not after it. **R-020** (the connection pool, entirely unconfigured) got ADR-039's ruling on 2026-09-03 and **deliberately stayed OPEN**: the decision is taken and the cross-tenant half is proven and guarded, but `pool.ts` is still six lines and `001_roles.sql` still sets no timeout — a ruling is a decision, not a configuration. **R-043** (new 2026-09-03, alongside ADR-055) is the one that cannot be closed by writing code: the platform will issue invoices as a legal entity and register none with سامانه مودیان, and ADR-048 part 5's deferral names a trigger — *the slice that integrates with سامانه مودیان* — that **belongs to no phase**, so nothing will ever pull it. **R-030 moved `OPEN` → `ACCEPTED` on 2026-09-03** when ADR-041 was ruled — the register's word for a risk retained by an evaluated decision, deliberately not `RESOLVED`: ledger growth is now owned and bounded by a stated trigger (50M rows / 50 GB, or R-025 closing) and is **not reduced**, since nothing is archived or deleted. **R-042** (new 2026-09-03) is the one to read before touching partitioning: a parent-only RLS policy does **not** protect a partition, and this project's `ALTER DEFAULT PRIVILEGES` grant makes every partition directly reachable by `nexora_app` — proven, not inferred, with cross-tenant read, insert, update and delete all succeeding in a probe. Nothing is partitioned today, so nothing is exposed today. **R-036 moved OPEN → RESOLVED on 2026-09-03** when ADR-051 was implemented: the contract it was opened over is now both defined and obeyed by running code. **R-008 was edited the same day and deliberately stayed OPEN** — its failure has never been reproducible on demand (165 local attempts), six occurrences arrived on unscheduled pushes, and **one green run proves nothing**; the row names an observation window (twenty consecutive green runs on `main`, or two weeks) instead of declaring victory. The two are not inconsistent: an undefined *contract* is eliminated by being defined and obeyed, an intermittent *failure* is a claim about behaviour over time. **R-037 stayed OPEN and lost only its example** — `SESSION_INVALIDATED` is now thrown and declared by nine capabilities, but nothing still enforces *documented → declared/thrown*, which is the row's actual finding. **R-041** (total cluster loss, opened 2026-09-03 on ADR-054's recommendation) is unrelated to **ADR-041** and shares its digits by accident. Read the row before assuming a gap is unknown or already fixed; the register's own preamble defines what each status word means here.
- **CI is green on `main`** as of run `33560478199` (`ee6cb68`), which post-dates the red run `33555601029` that earlier versions of this line reported. **R-008's root cause is DETERMINED as of 2026-09-02** and the row's central claim was wrong: there were never two failure modes. All four occurrences failed at `expect(losses)`, not `expect(successes)` — proven from the archived Vitest stack frames (`:452:20`, `:464:20`, `:464:20`, `:519:20`, all column 20, which is `losses` and not `successes`) rather than from the AssertionError message, which is byte-identical for both. So exactly one request returned 200 every time, and the loser returned `403` from `OrganizationAccessGuard`, which the test's `losses` filter does not accept. The "unexplained" mode was a misread line number that three investigations inherited, and it is why D-6's 165 local reproduction attempts found nothing — they were hunting a world that never occurred. **The test can still fail intermittently, and the fix is blocked on `R-036`** (which of `401`/`403`/`409` is correct for a membership revoked mid-flight): widening the test's accepted codes would silently decide that contract. R-036 is now R-008's sole live thread, and R-008 cannot close before it. **F-3 (connection-pool saturation) is refuted with measurements**, not just argument: the first `pg_stat_activity` sampling this repository has ever done shows the full local suite peaking at **23 connections against `max_connections` 100** at 7 Vitest workers, and CI runs fewer workers still. Those numbers are evidence for **ADR-039**, which is OPEN and had none.
- R-020 … R-031 come from `EXTERNAL_ARCHITECTURE_REVIEW_2026-08-28.md` (sixteen findings F-1 … F-16, verified one by one; three were materially corrected or refuted on inspection). R-014 … R-019 come from `PHASE_2_DOCUMENTATION_GAPS_2026-08-28.md`. Both are dated records — read the finding before acting on a row derived from it.
- A PostgreSQL deadlock/serialization failure now reaches the client as `CONCURRENCY_CONFLICT`/409 (R-008's candidate mitigation, done independently of R-008's own root cause), not `INTERNAL_ERROR`/500.
- Test counts, by what they measure: `npm test` reports **495 test cases passing across 52 files** as of 2026-09-05 (`npm test` 2>&1, authoritative runtime count). `PROJECT_GRAPH.md`'s mechanically-extracted figure is lower (473, `npm run graph`) because its static parser counts one `it.each([...])` call site as one case, not one per generated case. Both are correct for what they measure.
- **Phase 2 is being built. Items 1 and 2 are done as of 2026-09-05** — `plan.list` (the reference slice, reviewed and approved) and `prices`/`price_versions` (the first item that surfaces no capability). **Item 3 (the shared idempotency store, ADR-009) is done as of 2026-09-05** — the second infrastructure item and **the first tenant-owned table Phase 2 creates**. **11 capabilities, 12 routes** (one is `/health`, not a capability), **9 modules, 20 tables** (7 with RLS), **24 migrations**, conformance 0 violations. `PROJECT_STATUS.md` carries the per-slice narrative, including the infrastructure-item pattern six later Phase 2 items inherit. A separate `npm run check:partitions` (`tools/schema/check-partition-isolation.ts`, in CI since 2026-09-03) fails the build if any table partition lacks its own RLS/FORCE/policy or is directly reachable by `nexora_app` — it matches nothing today because nothing is partitioned, and exists because a partition is **not** protected by its parent's policy (`R-042`). A separate `npm run check:fk` (`tools/schema/check-cross-module-fk.ts`, in CI since 2026-09-05) fails the build on any foreign key that crosses a module boundary — `04` §1 and `PHASE_2_BRIEF.md` §5 both forbid one and **nothing enforced it until item 3**; it matches nothing today (all thirteen existing FKs are intra-module) and exists because item 4's `subscriptions` is the first slice that will want one. A separate `npm run check:register` (`tools/register/check-register.ts`, in CI since 2026-09-02) fails the build on any `RISK_REGISTER.md` row that does not render exactly eight cells or does not occupy one physical line — deliberately **not** a conformance rule, since ADR-030 scopes that harness to architecture and schema; it follows the `graph`/`openapi`/`dist-deps` convention for artifact-integrity checks instead.

- **Forty-four competitive rulings were recorded on 2026-09-04** (`decisions/2026-09.md`), from a read-only review of one live competitor — **a generalisation from a single observed instance, not a comparative study**, and the amendments carry that limit. **No new ADR was needed**: seven existing ADRs gained dated amendments (**ADR-023** `supportsInstallment`, false in V1; **ADR-024** one billing cadence and *the platform pays for lifecycle notifications*, because ADR-024 item 10 makes them mandatory and a mandatory obligation must not depend on a tenant's SMS credit; **ADR-025** plan-change direction determined from entitlements and quotas **never from price**, plus a prohibited *incomparable* case and `PLAN_CHANGE_INCOMPARABLE` in `05` §7; **ADR-027** two domain-connection methods with record-based the default; **ADR-028** tenant subdomains on a separate registrable domain and a generated subdomain for trials; **ADR-047** each term length its own price version with the discount **stored, never computed**; **ADR-052** the trial moves **7 → 14 days**). **Twelve competitive gaps were each given a phase in `06_IMPLEMENTATION_PLAN.md`** rather than in an ADR — the same fence by which ADR-048 could not add a table to `PHASE_2_BRIEF.md` §4 and ADR-057 could not add a capability to §3. **The revenue model, the red lines and the marketing claims are in `decisions/2026-09.md` and deliberately in no ADR**: a document whose value is stability is the wrong home for a price. **Two premises in the input were corrected rather than recorded:** ADR-027 item 4 does **not** exclude wildcards absolutely — it already says *"for tenant-owned domains"* and already permits `DNS-01` *"where the platform controls the zone"* — so the wildcard ruling is a **confirmation, not a narrowing**; and ADR-052's 7-day trial is justified against a **commercial specification outside this repository**, which still says 7 and is now out of step. **`PHASE_2_BRIEF.md` §9.13** records the V1 quota list as a **closed** list (`members`, `stores`, `domains`) — storage and bandwidth neither enforced **nor advertised** — plus two item-1 `plan_features` seeds. **Item 1's migration is unchanged** (ب-3), which is the condition Phase 2 slice 1 was waiting on. **Eleven further rulings arrived afterwards and were recorded on 2026-09-05** (sections ح and خ; the rulings file's running total is 55). **Again no new ADR**, and for a stronger reason than the first batch: **shipping has no port, no adapter and no ADR**, so ruling its design now would fix answers to questions the first slice has not asked. All eleven went to `06_IMPLEMENTATION_PLAN.md` as a **second dated 2026-09-04 amendment** — **ح-2 in Phase 2.5** (an operator capability to *publish* a plan version and a price version; the word is publish, never edit, because `plan_versions` is immutable) and **ح-1 plus all nine خ rulings in Phase 3**, beside ث-5's carrier port. **ح-1's rule is that only a prepaid shipping method may put a freight amount into an order total, a payment intent or an invoice** — for پس‌کرایه the platform never receives that money, and `invoices` is append-only, so a figure never collected could never be corrected out later. **The trap it exists to prevent: do not add a پس‌کرایه flag to ADR-023's `PaymentProviderCapabilities`**, which would put money the platform never touches inside the payment domain. **R-032 gained an owner and a deadline and deliberately stayed OPEN** — a phase is not a delivery, and adding a plan writer is a contract change, since `05` §4.2 names none. **A process failure is also on the record:** after session 18 pushed, an analyst copy overwrote three root files; **nothing normative was lost** and the repair is the 2026-09-05 entry.

Update this section, and `PROJECT_STATUS.md`, when that changes — new slice narrative goes in `PROJECT_STATUS.md`, not here.

## Skills

- `/new-slice` — implement one vertical capability slice mirroring the golden path. Use for every new capability.
- `/phase-gate` — audit a phase against its exit criteria before the next one opens.
- `/project-graph` — regenerate and read the mechanically-extracted map of what the repository contains: modules and their real dependency edges, tables and their RLS posture, capabilities, routes, singletons, ADRs, tests by layer. Run it at the start of a task, to avoid rediscovering the same facts by reading files, and after finishing a slice, to see what changed structurally.

## Always verify before reporting done

```bash
npm run typecheck && npm run lint && npm run format:check && npm test && npm run conformance && npm run db:migrate
```

Run `npm run db:migrate` against a database reset to empty when a migration changed, to confirm every migration still applies cleanly together, not just the new one in isolation.

Never weaken a conformance rule or add an `exceptions.json` entry to reach green.

## Local database note

This machine has **no Docker**. A native PostgreSQL 17 runs on **port 5432** (not `docker-compose.yml`'s `5433`), carrying the `nexora` database and both roles. `docker compose up` is the CI path only — CI runs `docker-compose.yml` (`postgres:17-alpine`, port 5433) on a real GitHub Actions runner; this machine never runs it.

No manual `export` is needed: `cp .env.example .env` once, and Node 24's `--env-file-if-exists=.env` (wired into `start:dev`, `start`, `db:migrate`, `conformance`, `test`) reads `DATABASE_URL`/`MIGRATE_DATABASE_URL` from it automatically. `platform/config.ts`'s own hard-coded fallback still points at compose's `5433` (CI depends on that default; `.env.example`'s committed values are `5432` for this machine) — `AUDIT_DATABASE_URL` also lives here, commented out, defaulting to `DATABASE_URL`. `nexora_migrate` has no `CREATEDB`, so a from-empty migration run needs a database created by a superuser first.
