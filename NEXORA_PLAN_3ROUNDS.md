# نقشهٔ سه‌نوبتی اصلاح اسناد Nexora

> **سند غیرنرمتیو.** این فایل تصمیم نمی‌گیرد؛ برنامهٔ کار را ثبت می‌کند و به ADR یا ردیف ریسکی
> که صاحب هر تصمیم است ارجاع می‌دهد. عمداً در ترتیب خواندنِ `AGENTS.md` §۱ نیست — مثل
> `TECHNOLOGY_RADAR.md` و `COMPETITIVE_POSITION.md`. **به‌عنوان مرجع به آن استناد نکنید.**


**تاریخ: ۲۰۲۶-۰۹-۰۲ · مخزن هدف: `D:\Nexora`**

این سند جایگزین `05_SESSION_PLAN.md` در بستهٔ راهبردی `NEXORA_STRATEGIC_PACKAGE_2026-09-02` است.

هر ادعای این سند در تاریخ ۲۰۲۶-۰۹-۰۲ مستقیماً از `D:\Nexora` خوانده شده. **تاریخ بخشی از ادعاست** —
اگر مخزن بعد از این تاریخ حرکت کند، شماره‌های ADR و ریسک این سند باید دوباره اثبات شوند:

```bash
grep -o "^| ADR-[0-9b]*" 02_ADR_INDEX_NORMATIVE_DECISIONS.md | sort -u | tail -5
grep -o "^| R-0[0-9]*" RISK_REGISTER.md | tail -1
grep -c "^| R-0" RISK_REGISTER.md
```

مقادیر تأییدشده در آن تاریخ: بالاترین ADR **ADR-043**، بالاترین ریسک **R-037**، تعداد **۳۷**.

---

## ۰. احکامی که تا اینجا گرفته شده

این چهار مورد تصمیم‌گیری شده‌اند و در نوبت ۱ رسماً ثبت می‌شوند.

### حذف نرم
**هیچ ستون `deleted_at` در فاز ۲ اضافه نمی‌شود.**

شواهد یک‌طرفه است: هیچ قابلیتی در فاز ۲ چیزی حذف نمی‌کند، و حدود بیست ستونی که چیزی آن‌ها را ست
نمی‌کند، هرکدام یک تعهد دائمیِ درستیِ کوئری می‌سازند که هیچ قانون conformance نمی‌گیردش.
محرک بازگشایی: اولین قابلیتی که واقعاً حذف می‌کند.

### قیمت تمدید
**قیمت همهٔ مشترکین در تمدید به نسخهٔ قیمت جاری به‌روز می‌شود. هیچ‌کس روی قیمت قدیم قفل نمی‌ماند.**

دو ریزتصمیم همراهش:

1. قیمت در لحظهٔ **صدور فاکتور تمدید** قفل می‌شود (۳۰ روز پیش از سررسید، `ADR-024` بند ۴)، نه در
   لحظهٔ سررسید — وگرنه فاکتوری که صادر شده وسط راه عوض می‌شود.
2. همان اعلان سی‌روزه حامل اطلاع قیمت جدید است، که تعهد اطلاع‌رسانیِ `ADR-024` بند ۱۰ را هم می‌پوشاند.

### MCP
**منظور Model Context Protocol است. فاز ۲ چیزی نمی‌سازد، فقط مسیر را نمی‌بندد.**

چهار ADR مربوط به MCP از قبل پذیرفته شده‌اند و همه مال فاز ۹‌اند: `ADR-001`، `ADR-001b`،
`ADR-003`، `ADR-007`. معماری capability-محور از قبل بستر درست است.

### تخفیف و کد معرف
**فاز ۲.۵ — بعد از راه‌اندازی اولیه، ولی تصمیمش پیش از ساخت جدول فاکتور.**

بررسی شد: تخفیف روی اشتراکِ خودت در **هیچ فازی** وجود ندارد. آنچه در فاز ۳ به نام «کوپن» هست،
کوپن فروشگاه‌دار برای مشتری‌های خودش است — موضوعی کاملاً دیگر. «کد معرف» هم در کل مخزن صفر بار
ذکر شده.

---

## نوبت ۱ — قفل تصمیم‌های ستونی

**یک سشن · فقط مستندات · مسدودکنندهٔ کد**

هر چیزی که به‌زودی به یک مایگریشن تبدیل می‌شود و بعد از آن تغییرش گران است. مایگریشن‌ها فقط رو به
جلو هستند (`ADR-021` بند ۸) — پس شکل یک ستون، به‌محض اینکه جدولش ساخته شود، از یک تصمیم پیکربندی
به یک مهاجرت داده تبدیل می‌شود. **این تنها نوبتی است که جلوی کدنویسی را گرفته.**

### ADR-044 — متن نمایشی در جدول‌های فاز ۲ · OPEN

آیتم ۱ جدول‌های `plans`، `plan_versions` و `plan_features` را می‌سازد. **هیچ سندی نمی‌گوید این‌ها متن
قابل نمایش دارند یا نه** — بررسی شد، `05_API_CAPABILITY_CONTRACTS.md` برای `plan.list` هیچ شکل پاسخی
جز scope و risk تعریف نکرده. `ADR-042` فقط پیام‌های خطا را تعیین تکلیف کرده، نه متن محتوا.

چهار گزینه: بدون متن (فقط کلید ماشینی) · یک ستون متن ساده · نقشهٔ JSONB زبان‌ها · جدول ترجمهٔ جدا.
توصیه: گزینهٔ اول، با JSONB به‌عنوان راه فرارِ نام‌برده‌شده.

**مسدود می‌کند: آیتم ۱ · حکم با توست**

### ADR-045 — کنترل هم‌زمانی خوش‌بینانه · OPEN

نگاشت `CONCURRENCY_CONFLICT`/۴۰۹ فقط وقتی کار می‌کند که خودِ PostgreSQL خطا بدهد. یک
read-modify-write در `READ COMMITTED` هیچ خطایی نمی‌دهد و بی‌صدا یک نوشتن را گم می‌کند.

**فهرست تصحیح‌شدهٔ جدول‌های قابل تغییر:** `subscriptions`، `tenant_over_limit_states`،
`billing_payment_intents`، `idempotency_records` — و **بدون** `outbox_events`، که پیش‌نویس بسته
اشتباهاً واردش کرده بود در حالی که آن جدول فقط‌افزودنی است.

همین خطا یک سؤال واقعی را لو داد که باید *نام برده* شود، نه حل: اگر `outbox_events` قابل ویرایش
نیست، وضعیت تحویل کجا می‌نشیند؟ (صاحبش آیتم ۱۴ و `ADR-050` است.)

**مسدود می‌کند: آیتم‌های ۴، ۸، ۱۲ · حکم با توست**

### ADR-046 — سازوکار حذف نرم · ACCEPTED

حکمش گرفته شده. نوشته می‌شود به همان سبکی که `ADR-042` نوشته شد — یعنی صریحاً توضیح می‌دهد
**چرا این یکی ACCEPTED است و نه OPEN**. نکتهٔ منطق سه‌ارزشیِ RLS در آن می‌ماند و برچسب «تأییدنشده»
می‌خورد، طبق سابقهٔ `ADR-041`.

**هیچ‌چیز را مسدود نمی‌کند · `ADR-020` و `ADR-026` دست‌نخورده می‌مانند**

### ADR-047 — قیمت‌گذاری در تمدید · ACCEPTED · موضوع جدید

> **این شماره در بستهٔ راهبردی برای موضوع دیگری (بازیابی مستأجری) رزرو شده بود؛ آن ADR نوشته نمی‌شود.**

با حکم تو نوشته می‌شود: تمدید به نسخهٔ قیمت جاری سنجاق می‌شود، قیمت در لحظهٔ صدور فاکتور قفل
می‌شود، و اعلان سی‌روزه حامل قیمت جدید است. باید تعاملش با `ADR-024` بند ۵ (تمدید زودهنگام هرگز
نباید مدت را کوتاه کند — با کدام قیمت؟) و با `ADR-025` (تسهیم نسبی) صریح گفته شود.

**مسدود می‌کند: آیتم ۲ و آیتم ۱۴**

### ADR-048 — شماره‌گذاری فاکتور · OPEN · موضوع جدید

بررسی شد: **در کل مخزن هیچ‌چیز دربارهٔ شماره‌گذاری فاکتور نوشته نشده.** سه سؤال که به‌محض ساخته‌شدن
جدول به تعهد اسکیما تبدیل می‌شوند: دنبالهٔ شماره برای هر مستأجر است یا سراسری؟ بی‌شکاف یا با شکاف؟
زیر صدور هم‌زمان چه اتفاقی می‌افتد؟

**تنش واقعی این ADR:** شماره‌گذاری بی‌شکاف و تلاش مجددِ کنترل هم‌زمانی (`ADR-045`) در جهت مخالف هم
می‌کشند — یک تراکنش شکست‌خورده یا شماره را می‌سوزاند یا دنباله را قفل می‌کند. همین انتخاب، پیش‌نیاز
حسابداری دوطرفه است.

**مسدود می‌کند: آیتم ۱۳ · حکم با توست**

### ADR-049 — آمادگی MCP · ACCEPTED · موضوع جدید

کوچک‌ترین ADR این نوبت، و مقیدکننده‌ترینشان. **چیزی نمی‌سازد** — اعلام می‌کند که هیچ‌چیزی که در فاز ۲
ساخته می‌شود نباید مسیر MCP فاز ۹ را ببندد، و *نام می‌برد* چه کارهایی آن را می‌بندند: منطق کسب‌وکار
داخل کنترلر، قابلیتی که فقط از راه HTTP قابل فراخوانی باشد، یا `CapabilityDefinition` که تک‌منبعِ
حقیقت نماند. بخشی از این‌ها از قبل در `AGENTS.md` §۴ ممنوع است؛ این ADR آن‌ها را به یک دلیل واحد
وصل می‌کند.

**هیچ‌چیز را مسدود نمی‌کند · هیچ کدی نمی‌نویسد**

### ADR-050 — رویدادهای مالی و مسیر تحویل بیرونی · OPEN · موضوع جدید

این ADR درِ حسابداری را باز نگه می‌دارد. بررسی شد: **هیچ‌جای مخزن شکل بدنهٔ رویدادهای `outbox_events`
تعریف نشده**، در حالی که `ADR-024` بند ۹ از قبل الزام کرده که `SubscriptionExpired` از همین مسیر
بیرون برود. بدنهٔ رویداد قراردادی است که هر مصرف‌کنندهٔ آینده به آن وابسته می‌شود و هیچ‌کدامشان بعداً
نمی‌توانند دوباره سرِ آن مذاکره کنند.

سه چیز را تعیین تکلیف می‌کند:

1. **شکل بستهٔ رویداد** — هر مبلغ باید `MoneyDto` باشد نه عدد خام (`ADR-022` بند ۷).
2. **محل وضعیت تحویل** — همان سؤالی که خطای پیش‌نویس `ADR-045` لو داد، چون جدول فقط‌افزودنی است.
3. **این قاعده که هر یکپارچگی بیرونی فقط از این مسیر مصرف کند**، نه با خواندن مستقیم از جدول‌ها.

**مسدود می‌کند: آیتم ۱۴ · پیش‌نیاز هر یکپارچگی حسابداری · حکم با توست**

### ردیف‌های ریسک — همان سشن

**R-038 — بازنویسی‌شده، باریک‌تر از پیش‌نویس بسته.**
ادعا این *نیست* که پنجرهٔ برگشت‌پذیر `ADR-020` مکانیزم ندارد — آن غلط است، چون در طول پنجره هنوز
چیزی حذف نشده. ادعای درست: **هیچ مسیر بازیابی برای یک purge اشتباه یا هر از‌دست‌رفتن دادهٔ
تک‌مستأجری وجود ندارد.**

در همین ردیف دو تعهد بی‌صاحب هم ثبت می‌شود که در بدنهٔ خود `ADR-020` پیدا شد: بند ۶ خروجی‌گرفتن
دادهٔ مستأجر را الزام کرده و هیچ فازی صاحبش نیست، و بند ۷ مستندکردن پنجرهٔ نگهداری بکاپ را الزام
کرده و آن هم صاحب ندارد.

**و یک قید معماری که شکل هر بازیابی را از پیش تعیین می‌کند.** جدول‌های دفترکل‌شکل (`invoices`،
`usage_ledger_entries`، `audit_events` و پنج تای دیگر) با `REVOKE UPDATE, DELETE FROM nexora_app`
محافظت می‌شوند. یعنی نقش برنامه *نمی‌تواند* ردیف دفترکل را حذف کند — پس بازگرداندن یک مستأجر هرگز
نمی‌تواند یک «عقب‌بردن» ساده باشد و باید بیرون از برنامه و با نقشی دیگر انجام شود. برای بخش‌های
قابل تغییر بازگردانی است، برای دفترکل‌ها فقط ثبت جبرانی.

**R-039 — رشد بی‌حد جدول `sessions`.**
پیش‌نویس بسته درست بود و بدون تغییر استفاده می‌شود: بدون سیاست نگهداری، بدون کار پاک‌سازی، بدون
`tenant_id` و RLS، هیچ آیتم فاز ۲ لمسش نمی‌کند، و در دامنهٔ `ADR-041` نیست.

**R-040 — مسیر شروع تست رایگان وجود ندارد.**
حالت `TRIALING` در ماشین حالت هست و کار `trial.expire` هم تعریف شده، ولی در فهرست پانزده قابلیت
**هیچ قابلیتی تست را شروع نمی‌کند** — در حالی که پیشنهاد جذب مشتری‌ات هفت روز تست رایگان است.

### خارج از دامنهٔ نوبت ۱

- هیچ مایگریشن، جدول، ماژول یا قابلیتی ساخته نمی‌شود.
- هیچ ADR پذیرفته‌شده‌ای ویرایش نمی‌شود.
- بیلد قرمز `membership-revoke` دنبال نمی‌شود — آن `R-008`/`R-036` است، متناوب و از قبل موجود.
- تخفیف و کد معرف اصلاً باز نمی‌شوند — نوبت ۲.

### شمارش‌ها — معیار پذیرش نوبت ۱

| سنجه | الان | بعد از نوبت ۱ |
|---|---:|---:|
| تعداد کل ADRها | 46 | **53** |
| ADR پذیرفته‌شده | 35 | **38** |
| ADR باز | 3 | **7** |
| ردیف ریسک | 37 | **40** |
| ردیف ریسک باز | 30 | **33** |

به‌علاوه: اصلاح خط کهنهٔ preamble رجیستری ریسک (می‌گوید «۳۱ ردیف» و جدول ۴۰ ردیف دارد) با یک
تصحیح تاریخ‌دار نه ویرایش خاموش؛ به‌روزرسانی شمارش‌های `CLAUDE.md` همراه تاریخشان؛ و یک ورودی در
`decisions/2026-09.md`.

---

## نوبت ۲ — تعیین صاحب فاز برای سه قلمرو بی‌خانمان

**یک سشن · فقط مستندات · غیرمسدودکننده**

این نوبت **طراحی نمی‌کند**. سه قلمرو را که امروز در هیچ فازی صاحب ندارند به یک فاز نسبت می‌دهد و
دامنهٔ هرکدام را می‌بندد.

خروجی‌اش یک ADR نیست — یک **اصلاحیهٔ تاریخ‌دار روی `PHASE_2_BRIEF.md`** است، چون دامنهٔ فاز مالِ سند
فاز است (`AGENTS.md` §۱ مرجع شمارهٔ ۲) و دامنه‌اش روی ۲۷ جدول و ۱۸ آیتم قفل شده. اضافه‌کردن قابلیت
به آن از راه یک ADR، دور زدنِ همان مرجعیت است.

### قلمرو ۱ — تخفیف و کد معرف

- **تخفیف:** جمعی (مثلاً همهٔ مشترکین یک پلن) و موردی (یک مشترک خاص) — با یک سازوکار یا دو تا؟
- تخفیف روی خط فاکتور می‌نشیند، یا اعتباری جداگانه است که موقع صدور کم می‌شود؟ این انتخاب شکل
  جدول فاکتور را تعیین می‌کند.
- **کد معرف:** خود کد، انتساب «چه کسی چه کسی را آورد»، و اعتبار کسب‌شده.
- پاداش با تعداد معرفی‌ها بالا می‌رود؛ سقف دارد یا نه؟

### قلمرو ۲ — بکاپ و بازیابی تک‌مستأجری

خواستهٔ روشن: اگر یکی از ۵۰۰ مشترک به مشکل خورد، فقط همان یکی برگردد. امروز هیچ مسیری برایش نیست و
تنها گزینه بازگرداندن کل کلاستر است — که ۴۹۹ نفر دیگر را هم عقب می‌برد.

- **خروجی‌گرفتن پیش از عملیات مخرب** — بند ۶ `ADR-020` از قبل «قابلیت خروجی داده با سهمیهٔ خودش» را
  الزام کرده و هیچ فازی صاحبش نیست. همین بند نیمی از راه بازیابی است و باید صاحب بگیرد.
- **بازیابی چه معنایی دارد** — طبق قید بالا، برای بخش‌های قابل تغییر بازگردانی است و برای دفترکل‌ها
  فقط ثبت جبرانی. هر دو باید نام‌گذاری شوند، وگرنه بعداً «بازیابی» چیزی وعده می‌دهد که نمی‌تواند بدهد.
- **چه کسی اجرایش می‌کند** — چون نقش برنامه اجازهٔ حذف ردیف دفترکل ندارد، این یک عملیات اپراتوری
  بیرون از برنامه است. پس نیازمند ممیزی مستقل و مجوز جداگانه است.
- **تمرین بازیابی** — بکاپی که هرگز برگردانده نشده بکاپ نیست. آیا تمرین دوره‌ای الزام می‌شود؟
- **مستندکردن پنجرهٔ نگهداری برای مشترک** — بند ۷ `ADR-020`، بی‌صاحب.

### قلمرو ۳ — یکپارچگی با نرم‌افزار حسابداری

> **دو یکپارچگی کاملاً متفاوت‌اند و نباید قاطی شوند.**

**حسابداری خودت** — درآمد تو از ۵۰۰ مشترک، فاکتورهایی که تو صادر می‌کنی. این نیمه از قبل در فاز ۲
زندگی می‌کند: جدول `invoices` در آیتم ۱۳، شماره‌گذاری در `ADR-048`، و مسیر خروج رویداد در `ADR-050`.
اینجا فقط باید تصمیم گرفت که همگام‌سازی دوطرفه چه چیزی را می‌نویسد و چه چیزی را فقط می‌خواند.

**حسابداری مشترک** — فروش فروشگاه او، وصل به نرم‌افزار حسابداری خودش. این نیمه *قابل طراحی نیست تا
فاز ۳ نیامده*، چون سفارش و فروش هنوز اصلاً در پلتفرم ثبت نمی‌شود. نمی‌شود داده‌ای را همگام کرد که
وجود ندارد.

پس آنچه *الان* بدهکار است فقط رزرو درز است:

- `webhook_endpoints` و `webhook_deliveries` در بستهٔ اسناد هستند و هیچ فازی صاحبشان نیست. تعیین
  صاحب، کل کار این قلمرو در نوبت ۲ است.
- و این قاعده که هر مصرف‌کنندهٔ بیرونی فقط از مسیر `ADR-050` بخواند — که اگر الان ثبت شود، اتصال
  حسابداری در فاز ۳ یک افزودن است نه یک بازطراحی.

### ⚠ ضرب‌الاجل — تنها ضرب‌الاجل سختِ کل نقشه

نوبت ۲ باید **پیش از آیتم ۱۳** (ساخت جدول‌های `invoices` و `invoice_lines`) تمام شود. بعد از آن،
اضافه‌کردن خط تخفیف به فاکتور یک مهاجرت داده است، نه یک تغییر ساده.

---

## نوبت ۳ — طراحی

**یک تا دو سشن · بعد از حکم نوبت ۲**

طراحی هر قلمروی که نوبت ۲ نزدیک اعلامش کند. تخفیف و معرف اول‌اند، چون ضرب‌الاجلشان از همه سخت‌تر
است. ترتیب داخلیشان هم اجباری است و سلیقه‌ای نیست: **اول تخفیف، بعد معرف** — چون پاداش معرف به‌شکل
تخفیف پرداخت می‌شود، پس معرف بدون مدل تخفیف قابل طراحی نیست. بکاپ و بازیابی، اگر نوبت ۲ نزدیکش
بداند، نوبت طراحی جداگانهٔ خودش را می‌گیرد.

### مدل تخفیف

باید با سه چیزی که از قبل پذیرفته شده‌اند سازگار باشد:

- پول هرگز عدد خام نیست و همیشه `MoneyDto` با واحد است (`ADR-022`).
- تسهیم یک مبلغ روی چند خط باید از تخصیص‌دهندهٔ باقی‌مانده‌پخش‌کن استفاده کند تا جمع اجزا دقیقاً
  برابر کل شود (`ADR-022` بند ۵).
- تعامل با تسهیم نسبیِ تغییر پلن (`ADR-025`) باید صریح باشد.

تخفیفی که با گرد‌کردن مستقل حساب شود، فاکتور را ناتراز می‌کند.

### کد معرف

سه چیز که باید همان‌جا حل شوند و نه بعداً:

- **انتساب** — چه کسی چه کسی را آورد و چه وقت قطعی می‌شود (لحظهٔ ثبت‌نام یا لحظهٔ پرداخت؟)
- **انباشت** — اعتبار چطور جمع می‌شود و در دورهٔ بعد چطور اعمال می‌شود.
- **تقلب** — معرفی‌کردن خود، حساب‌های ساختگی، و معرفی دوطرفهٔ حلقوی. اگر این همان‌جا حل نشود، بعداً
  به‌صورت زیان مالی کشف می‌شود، نه به‌صورت باگ.

هر دو زیر قواعد موجود می‌روند: هر نوشتن ایدمپوتنت است (`ADR-009`) و هر اعطای تخفیف ممیزی می‌شود
(`ADR-034`).

---

## ترتیب اجرا

| کِی | چه |
|---|---|
| **الان** | **نوبت ۱** — هفت ADR، سه ردیف ریسک، اصلاح شمارش‌ها. تنها کار مسدودکننده. |
| سپس | **چهار حکم با تو** — ADR-044، ADR-045، ADR-048، ADR-050. بدون این‌ها اسلایس ۱ و ۲ نباید شروع شود. |
| سپس | **بیلد قرمز** — اجرای آزمونی که در تحقیق R-008 نام برده شده و هرگز اجرا نشده. نتیجه هرچه باشد ثبت می‌شود. |
| سپس | **اسلایس ۱ و ۲** — `plan.list` و سپس `prices`. اولین کدی که پس از این همه نوشته می‌شود. |
| **پیش از آیتم ۱۳** | **نوبت ۲** — تعیین صاحب فاز برای سه قلمرو. ضرب‌الاجل سخت. |
| پس از آن | **نوبت ۳ و فاز ۲.۵** — طراحی، و سپس ساخت. |

---

## چهار حکمی که بعد از نوبت ۱ با توست

نوبت ۱ این‌ها را می‌نویسد و پیشنهاد می‌دهد، ولی تصمیم نمی‌گیرد. تا وقتی جواب ندهی، اسلایس ۱ و ۲
نباید شروع شوند.

| ADR | سؤال |
|---|---|
| `ADR-044` | پلن‌ها اصلاً نام نمایشی دارند؟ اگر بله، یک زبان یا نقشهٔ چندزبانه؟ |
| `ADR-045` | جدول‌های قابل تغییر ستون نسخه بگیرند یا نه — و اگر بله، دقیقاً کدام‌ها؟ |
| `ADR-048` | شمارهٔ فاکتور: هر مستأجر یا سراسری، بی‌شکاف یا با شکاف؟ |
| `ADR-050` | بدنهٔ رویداد مالی چه چیزی حمل می‌کند، و وضعیت تحویل کجا می‌نشیند؟ |

---

## پیوست — سه خطای تأییدشدهٔ بستهٔ راهبردی

این‌ها در برابر مخزن راستی‌آزمایی شده‌اند. اگر پیش‌نویس‌های بسته بدون اصلاح کپی شوند، کار خراب می‌شود.

**۱. `outbox_events` در فهرست «قابل تغییر» `ADR-045`.**
`PHASE_2_BRIEF.md` §۵ آن را در فهرست `REVOKE UPDATE, DELETE` گذاشته، کنار `subscription_periods`،
`subscription_state_transitions`، `usage_ledger_entries`، `billing_payment_events`، `invoices`،
`invoice_lines`. یک ستون `version` آنجا فیلدی است که هیچ‌وقت به‌روزرسانی نمی‌شود — که خودِ توصیهٔ
`ADR-045` آن را نقص می‌نامد. پیش‌نویس هم‌زمان با سند فاز، با فایل `02_PROPOSAL_TRIAGE.md` خودِ بسته،
و با خودش تناقض دارد.

**۲. `R-038` و `ADR-047` بسته روی بدخوانی از `ADR-020` بنا شده‌اند.**
جدول حالت‌های `ADR-020` می‌گوید دادهٔ `OFFBOARDED` «برای پنجرهٔ نگهداری حفظ می‌شود، سپس purge»، و
پنجرهٔ برگشت‌پذیر بند ۳ یعنی `DELETION_REQUESTED` → ۳۰ روز → purge. **در طول پنجره هیچ‌چیز حذف
نشده**، پس برگشت‌پذیری با نگهداری تأمین می‌شود و به هیچ سازوکار بازیابی نیاز ندارد.

دو نکتهٔ دیگر که بسته ندیده بود:
- **بند ۶ از قبل قابلیت خروجی دادهٔ مستأجر را الزام کرده** («باید یک capability با سهمیهٔ خودش باشد،
  نه یک اسکریپت موردی»). پس گزینهٔ B در `ADR-047` بسته گزینهٔ جدید نیست — یک الزام پذیرفته‌شده است
  که **فاز صاحب ندارد**.
- **بند ۷ از قبل دربارهٔ بکاپ‌ها حکم داده** («بکاپ‌ها به‌طور طبیعی منقضی می‌شوند… پنجرهٔ نگهداری باید
  برای مستأجر مستند شود») — و آن تعهد مستندسازی هم صاحب ندارد.

**نتیجه: `ADR-047` بسته نوشته نمی‌شود.** به‌جایش `R-038` با ادعای باریک و درست نوشته می‌شود، و
شمارهٔ ADR-047 به موضوع قیمت‌گذاری تمدید می‌رسد.

**۳. عبارت‌پردازی جزئی.**
`02_PROPOSAL_TRIAGE.md` می‌گوید یک جدول `idempotency_keys` «هم‌اکنون یک نقض زندهٔ conformance است».
قانون و fixture آن وجود دارد؛ نقض وقتی زنده می‌شود که چنین جدولی ساخته شود. این عبارت از §۵ خودِ
`PHASE_2_BRIEF.md` به ارث رسیده — نیازی به ویرایش مخزن نیست.

---

## پیوست — تطبیق مدل تجاری با فاز ۲

منبع: `D:\طرح پیشنهادی\12_COMMERCIAL_PRICING_AND_AI_DIAMOND_ECONOMY_SPEC11.md` §۲ و §۶،
تطبیق‌شده در ۲۰۲۶-۰۹-۰۲. **بستهٔ راهبردی این فایل را یک‌جا به‌عنوان «اقتصاد الماس، فاز ۱۲» کنار
گذاشته بود** — که برای نیمی از فایل درست است و برای نیمهٔ محصولی‌اش غلط.

### آنچه خوب می‌خواند

معماری دقیقاً برای این مدل ساخته شده: اشتراک یک‌ساله با محاسبهٔ تقویمی، مهلت ۷ روزه، پنجرهٔ بازگشت
۳۰ روزه، صدور فاکتور تمدید ۳۰ روز قبل (`ADR-024`). واحد پول IRR با صفر رقم اعشار **خودش مثال رسمیِ
`ADR-022`** است. حالت `TRIALING` و کار `trial.expire` وجود دارند.

«۰٪ کارمزد از فروشگاه» یعنی تقسیم پرداخت و دفترکل چندفروشنده لازم نیست — یکی از شش موضوع فایل ۵
طرح پیشنهادی واقعاً منتفی است.

### چهار ناسازگاری

| # | مدل تجاری می‌گوید | فاز ۲ چه دارد | نتیجه |
|---|---|---|---|
| ۱ | قیمت تمدید سال دوم بالاتر (۶م → ۸ تا ۱۰م) | اشتراک نسخهٔ قیمت را سنجاق می‌کند؛ هیچ قاعده‌ای دربارهٔ تمدید نیست | → `ADR-047` |
| ۲ | ۷ روز تست رایگان | حالت و کار انقضا هست؛ **هیچ قابلیتی تست را شروع نمی‌کند** | → `R-040` |
| ۳ | سه افزونهٔ جداگانه با قیمت مستقل | جای افزونه در کد هست و **عمداً همیشه خالی** (D2-7)؛ `subscription_items` ساخته نمی‌شود | ثبت در لاگ تصمیم |
| ۴ | ۳۰ روز ضمانت بازگشت وجه | بازپرداخت در سطح درگاه پشتیبانی می‌شود و `billing_refunds` آیتم ۱۳ است، ولی **قابلیت بازپرداخت در فاز ۲ نیست** → دستی با ممیزی (`ADR-023` بند ۱۰) | ثبت در لاگ تصمیم |

**نکتهٔ محصولی، نه فنی:** «۸ تا ۱۰ میلیون» یک بازه است، نه یک قیمت. نسخهٔ قیمت باید یک عدد باشد.
بدون این عدد، `ADR-047` قابل حکم‌دادن نیست.
