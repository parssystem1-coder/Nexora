# Risk Register

Tracks known risks to the platform's correctness, security, or delivery timeline, per the phase-exit gates in `06_IMPLEMENTATION_PLAN.md` and the blockers in `07_ARCHITECTURE_GAP_REPORT.md`.

Two items from Phase 0 tooling below (one open, one resolved). Otherwise not populated yet — no feature code exists, so no implementation-level risk has surfaced beyond what `06_IMPLEMENTATION_PLAN.md`/`07_ARCHITECTURE_GAP_REPORT.md` already track. Populate further as risks are discovered during implementation.

| ID | Risk | Likelihood | Impact | Mitigation | Owner | Status | Opened |
|---|---|---|---|---|---|---|---|
| R-001 | `docker-compose.yml`'s PostgreSQL path (used by CI and by any developer without a local Postgres) has never actually been run — Docker was unavailable in the sandbox that built it, so the migration runner and live-DB conformance rules were verified instead against a native PostgreSQL 17 install as a stand-in (see `REPOSITORY_AUDIT_REPORT.md` §4.5). The underlying mechanism is proven; only the Docker/compose plumbing itself (image pull, port mapping, healthcheck, `docker compose up -d --wait` in CI) is unverified. | Low — the compose file is standard and small | Medium — if broken, CI's conformance job and any developer without local Postgres cannot run `npm test` | User to verify locally: `docker compose up -d && npm test`, confirm `.github/workflows/conformance.yml`'s equivalent step on first CI run. Confirmed 2026-08-22: no Docker install planned yet, native PostgreSQL remains the verification basis for now; `docker-compose.yml` stays committed as the CI/other-developer path regardless. All connection strings are read from `DATABASE_URL`/`MIGRATE_DATABASE_URL` (`platform/config.ts`), never hardcoded elsewhere — verified by grep. | User | **PARTIALLY CLOSED, 2026-08-24** — CI has now actually executed for the first time (run `32663271905`, first push to `origin/main`). That run was red, but for a real and now-fixed reason unrelated to the compose plumbing itself: the workflow never ran migrations before the test suite, so every DB-touching test failed with `relation "users" does not exist` against a container that was empty by construction — see `DECISION_LOG.md` 2026-08-24 and the commit that added the missing `npm run db:migrate` step. That first run nonetheless proved three things this row had listed as unverified: (1) `docker compose up -d --wait` starts the Postgres service and it is reachable on port 5433 — the step succeeded in 11s; (2) `npm ci` resolves `@node-rs/argon2`'s Linux prebuilt binary with no node-gyp fallback needed; (3) TypeScript compiles cleanly on `ubuntu-latest`/Node 24. Those three are now CLOSED. **Still OPEN**: a fully green run has not yet been observed — the fix that adds the migration step has been pushed but its own resulting run had not completed as of this update. Do not treat this row as fully resolved until a run passes end to end, including the live-DB conformance scan and the `graph`/`openapi` drift checks, none of which have ever executed on Linux before. | User | **CLOSED, 2026-08-24** — run "graph: a layer with no files is not a layer" (`07909e9`, CI run `32666989546`) passed end to end: `docker compose up -d --wait` started Postgres and 19 migrations applied from the empty compose database, 293 tests passed including the live-DB RLS suite, the live-DB conformance schema checker passed, and both `graph -- --check` and `openapi -- --check` passed on `ubuntu-latest`/Node 24. What this closes: the docker-compose Postgres path itself works end to end, not merely that its container starts. It is also stronger evidence than a green run alone would be, because it was not the first run — it followed two genuinely RED runs (`32663901973`, `32665788428`) that the harness correctly failed on real, distinct defects (a missing migration step; a locale-dependent sort; an empty-directory artifact) before this one went green. A CI job that has never once caught a real problem proves nothing about whether it *can*; this one already had, twice, before it passed. **Not folded into this closure, tracked separately:** R-005 (no rate limiting on `auth.login`) remains OPEN under its own id — unrelated to the compose/CI plumbing this row was about. **Correction, 2026-08-24 (gate review Finding 3):** this row's original description and every other document describing the local verification basis called native PostgreSQL 17 "a stand-in" for compose — accurate while compose was unverified, but it also silently meant every claim in this repository's history was verified on 17 while `docker-compose.yml` pinned `postgres:16-alpine`, a mismatch nobody had actually checked. `docker-compose.yml` now pins `postgres:17-alpine` to match, and this closure's own evidence (CI run `32666989546`) was re-confirmed on 17 in the commit that made this correction — see `DECISION_LOG.md`. | User | CLOSED | 2026-08-22 |
| R-003 | Tenant isolation for the two membership tables depends on an RLS clause keyed on `app.user_id`, not on `tenant_id` alone: `USING (tenant_id::text = current_setting('app.tenant_id', true) OR user_id::text = current_setting('app.user_id', true))`. It exists because pipeline steps 2–3 must read these tables *before* step 5 establishes tenant context (`08_PHASE_1_BRIEF.md` §2), so a `tenant_id`-only policy returns zero rows and no request can ever resolve store access. The clause therefore widens the policy beyond what §5 describes, and any bug in how `app.user_id` is set would widen it further. **Location (the instruction referenced `modules/tenant/infrastructure/store-memberships.rls.sql`, which does not exist — RLS lives in the creating migration per §5's "in the same migration" rule):** `modules/tenant/migrations/20260822090300_tenant__create_memberships.sql` and `20260822090500_tenant__create_store_memberships.sql`. `app.user_id` is set in exactly one place, `platform/db/tenant-context.ts`. | Low — the clause is narrow, applies to two tables only, and `app.user_id` is set by the single `@singleton-role: tenant-context` helper that `DB-ACCESS-TRANSACTION-BYPASSES-HELPER` forbids bypassing | High if wrong — it is the gate in front of the store access check, so a defect here is a cross-tenant read | Kept deliberately (`DECISION_LOG.md`, decided 2026-08-22): removing it disables the system rather than hardening it. Must not be copied to other tenant-owned tables without its own decision. Covered by tests asserting cross-tenant denial and RLS-fails-closed. **Task 2 re-evaluates a clause-free alternative** — a `SECURITY DEFINER` bootstrap function, or an authoritative tenant on the session row so `app.tenant_id` is known from step 1 and the bootstrap phase disappears (this couples to the `sessions` decision). **Update 2026-08-23 (`organization.create`, Task 2 slice 1 — documentation only; no policy changed and no migration in that commit):** the first code to *write* a `memberships` row does not need this clause. `organization.create` inserts the creator's membership inside a transaction where `app.tenant_id` is already the new organization's id, so the `tenant_id` half of the policy satisfies the insert on its own — `memberships` declares only `USING` with no `WITH CHECK`, so Postgres reuses the `USING` expression as the insert check. The clause's justification therefore narrows to one caller: `StoreAccessGuard`'s bootstrap *read*. That does not weaken the "removing it disables the system" finding, which was always about that read. **Realistic forcing point: slice 5 (`auth.login`).** Slices 2 and 3 (`membership.invite`, `membership.role.assign`) both run inside an already-established tenant context and never touch the bootstrap read, so neither will force a decision on its own. Of the two alternatives above, the session-row one couples directly to the `sessions`/`credentials` tenancy decision that slice 5 has to take anyway; the `SECURITY DEFINER` one is not coupled to it, but nothing before slice 6 forces it either. Resolving this at slice 5 should therefore be a deliberate choice rather than something discovered at the end of Task 2. **Update 2026-08-23 (`auth.login`, Task 2 slice 5 — the forcing point, evaluated deliberately, no policy changed): both alternatives evaluated on their merits and rejected.** (i) A `SECURITY DEFINER` bootstrap function remains uncoupled to the `credentials` tenancy decision this slice actually made (`credentials` joined the identity exemption, same as `sessions` — see `DECISION_LOG.md`) — nothing here forces building it, and it would trade the current narrow, already-tested RLS surface for a new privileged-function trust boundary with no offsetting need. (ii) "An authoritative tenant on the session row," the alternative this row named as coupled to slice 5, is not actually available even now that `sessions` exists in its final Phase-1 shape: `active_organization_id` is present, but ADR-002 and ADR-029 item 5 are explicit it is session state, a UI convenience, never authority for tenant resolution — trusting it as authoritative would mean either accepting a staleness window after a membership is revoked (a real regression against ADR-029's own "cached membership invalidates on membership change" verification item) or re-querying `memberships` to confirm it, which is the identical bootstrap read this clause already exists to permit, merely relocated. Neither alternative removes the need for the clause. | User + AI implementer | ACCEPTED — clause kept permanently by evaluated decision; both alternatives evaluated and rejected on their merits, not deferred further. **Corrected 2026-08-24** (was `RESOLVED`): the underlying risk — a policy wider than `tenant_id` alone — is retained by design, not eliminated; `RESOLVED` reads as "gone" to a future phase-gate review, which this is not. | 2026-08-22 |
| R-002 | A table's owning PostgreSQL role bypasses `ENABLE ROW LEVEL SECURITY` entirely by default (confirmed empirically). This Phase 0 scaffold originally used one role for both migrations and app queries, which would have silently violated `08_PHASE_1_BRIEF.md` §5 ("the application database role cannot bypass RLS") the moment real tenant-owned tables existed. | N/A — closed before any real migration existed | Would have been critical — silent cross-tenant data exposure, and every tenant-isolation test would have passed for the wrong reason | Three independent layers, all implemented and verified 2026-08-22, see `DECISION_LOG.md` "RLS: FORCE ROW LEVEL SECURITY or a non-owner app role": (1) role separation — `nexora_migrate` owns/migrates, `nexora_app` (NOSUPERUSER, NOBYPASSRLS, never an owner) is the only role the app/tests use; (2) `FORCE ROW LEVEL SECURITY` on every tenant-owned table, now a mechanical conformance rule (`SCHEMA-MISSING-FORCE-RLS`) in both the static and live-DB schema checkers; (3) a runtime assertion (`platform/db/assert-role-safety.ts`) that fails fast if the connected role could bypass RLS, wired into `platform/db/tenant-context.spec.ts`, and confirmed to actually fire against both a superuser and an owner-role misconfiguration. | AI implementer | RESOLVED | 2026-08-22 |
| R-004 | Store slug uniqueness (`04_DATABASE_BLUEPRINT.md` §5) is enforced per-organization — `UNIQUE (tenant_id, slug)` on `stores`, added in Task 1 and unchanged by `store.create` (Task 2 slice 4). But `reserved_subdomains` exists specifically because a store slug is expected to become a subdomain, and a subdomain namespace is inherently global, not per-tenant. Two different organizations can each hold a store slugged `shop` today — proven directly by `apps/api/store-create.integration.spec.ts`'s "the SAME slug succeeds in a DIFFERENT organization" test, which is asserting the *current, correct-per-04-§5* behavior, not a bug. | Low today — no subdomain routing exists yet, so the collision has no runtime consequence in Phase 1 | High at Phase 4 — ADR-028 requires deterministic host resolution (`UNIQUE (hostname_ascii) WHERE status = 'VERIFIED'`, platform-global) for *verified custom domains*; nothing in the docs pack states whether platform-provided subdomains (`{slug}.<platform-domain>`) are governed by that same rule or need their own. If they do, per-organization slug uniqueness and platform-global subdomain uniqueness are in direct tension, and two stores that validly share a slug today cannot both keep a `{slug}.<platform-domain>` address once one is assigned. | Not solved here, deliberately (`DECISION_LOG.md`, 2026-08-23, `store.create` decision 6) — ADR-028/ADR-019 host resolution is Phase 4, and `AGENTS.md` §4 forbids building for `future/`. Recorded so Phase 4 meets this as a known decision point: either (a) platform subdomains get their own global-uniqueness migration/reservation step at store-creation time once that feature exists, or (b) the platform subdomain feature is scoped out entirely in favor of custom domains only, in which case this risk closes without a schema change. No index changed, no resolution attempted. | User + AI implementer | OPEN — Phase 4 decision point | 2026-08-23 |
| R-005 | `auth.login` (Task 2 slice 5) ships with no rate limiting or lockout. ADR-029 item 2 requires "failed attempts are rate-limited per identifier and per IP"; `05_API_CAPABILITY_CONTRACTS.md` §5's `CapabilityDefinition` shape names a rate-limit field, and `modules/capability/domain/capability-definition.ts` deliberately omits it — the same "do not declare a field nothing enforces" reasoning already applied to `entitlements`/`quota`/`approval`. No module-local throttle was built as a substitute (`AGENTS.md` §4's idempotency reasoning applies in spirit: a rate limiter is a shared, cross-capability concern — `01_ARCHITECTURE_BASELINE_RFC.md` calls for it per tenant, per user AND per IP — not something one capability should invent its own version of). | Medium today — the endpoint is public and unthrottled, but Phase 1 has no production traffic; the enumeration side channel this same slice closes (indistinguishable failure codes and equalized timing) is the cheaper, already-mitigated half of the credential-stuffing risk surface | High once this is internet-facing — an unthrottled login endpoint is a standard credential-stuffing and brute-force target regardless of how well individual-attempt enumeration is closed | Deliberately deferred to a real, shared platform rate-limiting mechanism (per-tenant, per-user, per-IP, per `01_ARCHITECTURE_BASELINE_RFC.md`) — not scoped to this single Task 2 slice and not to be built as a one-off inside `modules/identity`. Must exist before `auth.login` is exposed to real, untrusted traffic. | User + AI implementer | OPEN — must be resolved before production exposure | 2026-08-23 |
| R-006 | `PermissionCheckRepositoryPg.hasPermission` (`modules/authorization/infrastructure/permission-check.repository.pg.ts`) joins `membership_roles` → `role_permissions` → `permissions` and does NOT filter by `memberships.status` — in isolation, it would return `true` for a REVOKED membership's still-present role grants. Discovered while implementing `membership.revoke` (DECISION_LOG.md 2026-08-24, decision 8), which deliberately does not delete a revoked membership's `membership_roles` rows and therefore depends on this gap never actually being reachable. | Low today — the only thing that ever produces the `membershipId` a permission check runs against is `ResolveOrganizationAccessService`, which already refuses a revoked membership with `FORBIDDEN` before any permission check is reached; no code path in this codebase presents a revoked membership's id to `hasPermission` | Medium if a future capability ever checks a permission for a membership id it did NOT itself resolve through that guard (e.g. checking a THIRD party's permission, not the caller's own) — it would silently succeed for a revoked membership | Not fixed here — `AGENTS.md` §4 forbids a broad refactor while implementing a feature, and this is shared authorization code, not `membership.revoke`'s own. The fix, when taken: add `.where("memberships.status", "=", "ACTIVE")` (with the necessary join) to `hasPermission` directly — defense in depth, not a behavior change under the current, only-ever-guard-verified call pattern. | User + AI implementer | OPEN — hardening, not urgent while the only caller is guard-verified | 2026-08-24 |
| R-008 | CI run `32987662296` (commit `09a8017`, 2026-08-26T16:18:38Z — code byte-identical to `fd13c1e`/`fbc7ba4` per `git diff --stat fd13c1e..fbc7ba4`, which touches only `.gitignore` and a dated report) failed one test: `apps/api/membership-revoke.integration.spec.ts`'s two-concurrent-owners test (`:439-459`). **Observed fact:** the failing assertion, `expect(successes).toHaveLength(1)` (line 451), received an empty array — by the test's own computation, neither of the two concurrent revoke requests was reported as returning HTTP 200 in this run. `PHASE_1_GATE_CONFIRMATION_2026-08-26.md` §2 had run this exact test 5/5 green locally on this same code just before it was cut; the later CI run on `fbc7ba4` (`33014271741`, docs/gitignore-only diff from the failing commit) was green. **What was checked and does NOT hold up, corrected from an earlier draft of this row:** an initial read of the job log attributed an `[HttpExceptionFilter] ERROR undefined` line to this failure, suggesting an unhandled 500/possible deadlock. On closer reading that ERROR line is timestamped ~16 seconds after this test's own execution window and sits immediately before `error-contract.integration.spec.ts` reports success — it belongs to that unrelated file's own deliberate 418-leak-sanitization test, not to this one. No `HttpExceptionFilter` ERROR log of any kind appears near this test's actual timestamps. Separately, the job log's structured per-request access-log lines bracketing this test's stdout markers show a `200` and a `403` for the same organization — but that reading is arithmetically inconsistent with the test's own report of zero 200-statuses, and concurrent-request log lines cannot be reliably ordered against test boundaries from the archived log alone. **The precise HTTP status codes the two requests received, and the mechanism that produced them, are UNDETERMINED from the available evidence** — this is a correction, not a refinement, of the earlier deadlock claim. A Postgres deadlock (40P01) from `lockActiveForUpdate`'s unordered `SELECT ... FOR UPDATE` (`membership.repository.pg.ts:38-47`, no `ORDER BY`) remains one structurally plausible UNVERIFIED hypothesis for how two symmetric concurrent transactions could produce an outcome outside the test's anticipated 200/409/401 set — stated explicitly as a hypothesis, not a diagnosis; no repro was attempted, since this task was documentation-only and read-only.

**Correction, 2026-08-28 — an unsound inference in the previous version of this row.** The previous text claimed "since neither request was measured as a 200, neither revoke committed." That does not follow, and is corrected here rather than left standing. Read `modules/tenant/interfaces/membership-revoke.controller.ts:85-134`: `recordAuditEventDurable` (line 116) is called **outside** the `try/catch` (lines 89-112) that wraps `withTenantContext`'s domain transaction. If the audit write throws — for any reason, on either the success or failure path — that exception propagates unguarded out of the handler and the client receives a non-200 response, **even though the domain transaction inside the `try` block already committed**. A second, previously unnamed candidate mechanism for this row is therefore: request A's revoke committed, then its own post-commit audit write threw, turning what was a real 200 into an observed non-200. This is exactly as unconfirmed as the deadlock hypothesis — named here because the controller's structure makes it real, not because there is evidence it is what happened.

**Also unrecorded, stated explicitly rather than left implied:** the failing assertion was at line 451 (`expect(successes).toHaveLength(1)`), which precedes the database-state assertion at line 459 (`activeOwners`). Vitest stops at the first failing assertion, so line 459 never executed in the failing run — **the actual end state of the `memberships` table in that CI run is unknown and was never recorded.** Neither "both requests failed, both memberships stayed ACTIVE" nor "one committed, its audit write then failed" can be confirmed or ruled out from what this run captured.

**Why R-007 stays CLOSED — on the correct ground, not the status codes.** The previous version of this row grounded R-007's safety in "neither response was a 200." That ground is unsound (above) and is withdrawn. The actual ground is the locking mechanism itself, independent of what any client ever saw: `lockActiveForUpdate` (`membership.repository.pg.ts:38-47`) takes `SELECT ... FOR UPDATE` over the tenant's entire ACTIVE membership set with no additional predicate, so the two concurrent transactions in this test necessarily contend for the same row lock. Under `PHASE_1_GATE_CONFIRMATION_2026-08-26.md` §2's traced `FOR UPDATE` re-check mechanism, the second transaction to acquire the lock re-reads the first's committed state before its own last-member/last-owner check runs — so both transactions cannot simultaneously observe "2 active owners" and both proceed. This holds regardless of what HTTP status either request's *client* ultimately received, including a case where a real commit is masked by a later, unrelated audit-write failure. **R-007 stays CLOSED on this mechanism, not on the observed status codes. R-008 stays OPEN, root cause still UNDETERMINED** — R-008 is a separate, narrower finding about an unexplained failure mode under concurrency, not a reopening of the last-owner race. **Correction to the record (carried here, not by editing the dated report):** `PHASE_1_GATE_CONFIRMATION_2026-08-26.md` §2's claim that this test was "demonstrated stable under load... 5/5 green, no flakiness" is now known to be an under-sample — the same code, same test, failed once in CI five hours before that confirmation's own commit went green elsewhere. That report is left unedited per this project's standing convention for dated records, the same way R-001 above carries its own PostgreSQL-16 correction rather than editing superseded text. **Consequence for future reviews:** a future red run on this specific test must not be assumed to be flake without investigation — the regression test guarding R-007 is itself intermittently failing for a reason not yet understood. | Low — reproduced exactly once across all runs observed | Medium — an intermittent, unexplained failure in the regression test for a previously-fixed critical race, with only an unverified candidate mechanism | Not investigated or fixed here (documentation-only pass). Candidate mitigations, named but not implemented: (1) add a deterministic `ORDER BY id` to `lockActiveForUpdate`'s query, closing the deadlock hypothesis if it is correct; (2) map Postgres `40P01`/`40001` (deadlock/serialization failure) to a documented error code instead of whatever currently reaches the client, as defense in depth regardless of root cause; (3) reconsider `recordAuditEventDurable` sitting outside `membership-revoke.controller.ts`'s `try/catch` — a throwing audit write can currently turn a genuinely committed revoke into a client-visible non-200, independent of whether either hypothesis above is what actually happened here. All three out of scope here. | User + AI implementer | OPEN — intermittent, root cause UNDETERMINED | 2026-08-27 |
| R-007 | `PHASE_1_GATE_REVIEW_2026-08-24-II.md` Finding 1: `RevokeMembershipService`'s last-owner/last-member checks were a read-then-write race with no database backstop — under READ COMMITTED (the only isolation level this codebase ever uses), two concurrent revoke requests against an organization's only two owners could each read "2 active owners" before either committed, and both proceed, leaving zero. A lower-severity sibling race let two concurrent revokes of the SAME target both pass the "not already revoked" check. | Was Low likelihood (needs two requests close enough together, no adversarial timing required) | Was High impact if triggered — the exact dead-end state ("zero active owners, no capability can ever reverse it") the guard exists to prevent | Fixed, not deferred: `MembershipRepository.countActive` replaced with `lockActiveForUpdate` (`SELECT ... FOR UPDATE` on every ACTIVE membership row in the tenant), called before either the "already revoked" or "last member" decision, so a concurrent revoke against the SAME tenant blocks on the row lock until the first transaction resolves, then re-reads post-commit state. Covers the owner-count race too without a second lock on `membership_roles`, because no capability ever removes a role grant (DECISION_LOG.md 2026-08-24, "membership.revoke: closing the last-owner/last-member race" — see that entry's reasoning for why `memberships.status` is the only column either count can change through). Proven by real concurrency, not simulated: `apps/api/membership-revoke.integration.spec.ts`'s "two concurrent revokes of an organization's only two owners" test fires two genuine HTTP requests with `Promise.all` and no `await` between them against a real running app and real PostgreSQL, and was confirmed to reproduce the defect pre-fix (`.forUpdate()` temporarily removed) before being kept as the regression proof. | AI implementer | CLOSED | 2026-08-24 |
