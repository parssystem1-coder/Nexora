# Project Status

Full per-slice narrative for Nexora's Phase 0/1 work, moved verbatim out of `CLAUDE.md` on 2026-08-27 to keep that file a stable entry point (see `decisions/2026-08.md`, 2026-08-27, "Cutting per-session context cost"). Nothing below was reworded or reordered in that move — it is exactly what `CLAUDE.md`'s own "Current state" section said, oldest first, before the split.

`CLAUDE.md` still carries a short pointer here and the always-current facts (skills, verification command, local-database note). This file is the detail.

---

Phase 0 complete. Phase 1 Task 0 and Task 1 (golden path `store.read`) complete and **repaired** — an independent audit found six defects (audit events not surviving their own transaction, a 404 returning `VALIDATION_ERROR`, three connection pools created at import time, `organizations`' RLS `WITH CHECK` covering UPDATE with no real predicate, `audit_events` not actually append-only) and all six are fixed as of "Repair item 6: audit_events is append-only for the app role, not just by convention" (`a726dfe`); see `PHASE_1_REPAIR_REPORT.md` for the defect-by-defect record.

Audit placement is now ADR-034 (`03` §3.1 and `08` §2 step 8 previously contradicted each other and both misdescribed the shipped design).

**Phase 1 step 4 done:** `modules/money` — `Money` value object (bigint minor units + explicit currency), the `currencies` registry, and the remainder-distributing allocator carrying the ADR-030 `money-allocator` singleton role. Per-currency minor units are read from the table, never hard-coded to 2.

**Task 2 slice 1 done:** `organization.create` (`POST /api/v1/organizations`). Creates the organization, an ACTIVE membership for its creator and an `owner` role grant in one transaction; no new tables. Two documented divergences from the golden path, both structural to a capability that creates its own tenant — no guard for steps 2–4, and no permission to assert at step 6. First slice whose domain transaction can actually fail, so it is where ADR-034's "the audit row survives a rolled-back transaction" is proven end to end. See `DECISION_LOG.md` (2026-08-23) for the five open questions it settled, one of which stays OPEN pending ADR-009.

**ADR-033 now in force:** `openapi.json` is generated from each capability's Zod schemas and `CapabilityDefinition` (`npm run openapi`, `-- --check` in CI). `CapabilityDefinition` carries `route`, `inputSchema`, `outputSchema` and `errorCodes`. Never hand-edit the artifact.

**Task 2 slice 2 done:** `membership.invite` (`POST /api/v1/organizations/{organizationId}/memberships`). Adds an existing platform user, found by email, as an ACTIVE member holding **no roles** — role assignment is slice 3, per `03` §167's flow. First slice with a real permission behind it (`membership.invite`, granted to `owner`+`admin` only, added by migration) and the first with an `OrganizationAccessGuard` doing steps 2–4 at organization level. No structural divergence from the golden path. `DECISION_LOG.md` (2026-08-23) records what "invite" can mean given no `invitations` table and no `PENDING` status — items 1 and 2 there are **OPEN for V1**, to revisit when the notification module and an accept flow exist.

**Task 2 slice 3 done:** `membership.role.assign` (`POST /api/v1/organizations/{organizationId}/memberships/{membershipId}/roles`). Adds one role to a membership's set (does not replace it) — first HIGH_WRITE capability, granted to `owner` only (not admin, unlike `membership.invite`'s owner+admin), sidestepping the self-escalation question rather than solving it with approval machinery that doesn't exist until Phase 9. Unknown role keys are rejected by a Zod enum over the closed platform role catalog (`ROLE_KEYS`, new in `modules/authorization/domain`), never reaching the database. `MembershipRepository` gained `findById`, which is **not** safe to trust without an explicit tenant check — `memberships`' self-access RLS clause (R-003) can surface a caller's own membership row from a *different* organization; the service checks `target.tenantId !== command.tenantId` explicitly and this is proven by a dedicated integration test, not just documented.

**Session invalidation on role change is now implemented** — the first of `08` §5's three triggers ("password change, membership revocation, role change") to be built. A new `SessionRevocationRepository` port (`modules/identity/contracts/`) revokes every ACTIVE session for the target user, in the same transaction as the role grant, so the two writes are atomic. This was a deliberate choice, not an obvious one: `membership.role.assign` is the *only* point among all six Task 2 slices where a role change can occur at all, since `membership.revoke` and any password-change capability are both absent from the six-slice list — deferring here risked the trigger never being built in Task 2's announced scope. The other two triggers (password change, membership revocation) remain **unimplemented** — see `DECISION_LOG.md` (2026-08-23, decision 9) before assuming session invalidation is fully covered.

Fixed along the way: `modules/identity/infrastructure/identity.tables.ts`'s `sessions.revoked_at` column was typed in a way (`ColumnType<...> | null` wrapped outside, not inside) that made it silently un-updatable through Kysely — nothing had ever updated it before this slice needed to.

**Task 2 slice 4 done:** `store.create` (`POST /api/v1/stores`, body `{ organizationId, name, slug }`). **First slice whose route breaks the path-nested-organizationId pattern** slices 2–3 established — `05_API_CAPABILITY_CONTRACTS.md` §6.1 gives this capability an explicit worked contract (flat route, `organizationId` in the body), the first Task 2 capability with one, and it was followed literally over the unwritten internal convention. `OrganizationAccessGuard` now reads `organizationId` from the path if present, falling back to the body otherwise — proven not to regress `membership.invite`/`membership.role.assign` by running their full existing suites unchanged. **Read this before adding a fifth capability**: there is no single routing convention across the four capabilities; each follows its own `05` contract where one exists (`organization.create`, `store.create` — both flat) and falls back to path-nesting where none does (`membership.invite`, `membership.role.assign`).

Creates the store, then a `store_membership` row for its creator in the same transaction — otherwise the creator could not `store.read` the store they just made (08 §5: org membership alone is insufficient for store access). Proven end to end: the integration suite drives `store.create` then `store.read` through real HTTP in one test, the first test in this codebase to exercise two capabilities together.

Adds `reserved_subdomains` (13 seeded names — the conventional infrastructure subdomains a platform withholds from itself; expected to grow before Phase 4) and a new `DOMAIN_RESERVED` error code (409, distinct from the duplicate-slug `CONFLICT`). Checked after slug normalization so the two cannot drift. `store.create`'s permission (owner+admin, matching `membership.invite`'s tier) is a new migration; no role added, so `role-catalog-agreement.spec.ts` is unaffected.

**RISK_REGISTER.md R-004 (new):** per-organization store-slug uniqueness (correct, per `04` §5) is in tension with `reserved_subdomains`' implication that a slug becomes a subdomain — subdomains are a global namespace. Flagged for Phase 4, not solved; the index is unchanged.

**Fixed after slice 4: a body field could silently override a path parameter.** `membership.controller.ts` and `membership-role.controller.ts` built their Zod input as `{ pathParams, ...body }` — body spread last — so a client-supplied body key with the same name as a path parameter won silently. For `membership.role.assign` this was a real defect: a body `membershipId` could name a DIFFERENT membership than the URL, and that membership — not the one in the path — is what got the role, the audit `resource_id`, and the audit metadata. **Read this before writing a fifth controller that mixes a path parameter with a body**: the rule now is that a path parameter is authoritative, and a body value for the same key that DIFFERS is rejected with `VALIDATION_ERROR` (an identical echo is accepted). Enforced by one shared function — `resolvePathOrBodyValue`/`buildValidationInput` in `modules/capability/interfaces/path-body-conflict.ts`, exported via `modules/capability/contracts/`, driven by `CapabilityDefinition.route.pathParams` rather than a hand-written list per controller. `organization.create`/`store.create` (`route.pathParams: []`) don't need it and aren't routed through it; `store.controller.ts` (golden path, GET, no body) doesn't exhibit the pattern and is untouched. See `DECISION_LOG.md` 2026-08-23.

**Task 2 slice 5, first of three, done:** `auth.login` (`POST /api/v1/auth/login`). **The most divergent capability from the golden path so far** — no guard runs before it (there is no session yet), no tenant/store is resolved, no permission is asserted, and **no transaction is opened at all**: `users`, `credentials` and `sessions` are all RLS-exempt, and the single `INSERT` into `sessions` needs no explicit transaction. `platform/db/tenant-context.ts` stays the only place a transaction is ever opened in this codebase — this capability simply never needs one. Full pipeline accounting in `DECISION_LOG.md` 2026-08-23, decision 1.

**`credentials` now exists and is RLS-exempt** (joins `users`/`sessions`/the role catalog — a password belongs to the person, not a tenant). This changed three normative sources in the same commit: `08_PHASE_1_BRIEF.md` §5's exemption list, `.claude/skills/new-slice/SKILL.md` Step 4's list, and `tools/conformance/rules/schema.ts`/`schema-live.ts`'s `TENANT_EXEMPT` sets — the harness actually flagged the gap the first time conformance ran against the new migration, confirming the check works rather than only ever passing. `identity_providers` remains unbuilt and undecided.

**ADR-035 (new): platform-scope audit events.** `audit_events.tenant_id` is `NOT NULL` with one RLS policy that a `NULL` value could never satisfy under any context — permanently unreadable, not just hidden. `auth.login` (global scope, no tenant) writes under a reserved sentinel, `PLATFORM_TENANT_ID = 00000000-0000-0000-0000-000000000000` (`modules/audit/domain/audit-event.entity.ts`), read back through the same `withTenantContext` helper every other tenant uses — no schema change, no second RLS branch. The sentinel is a storage device only: never logged, never in `request.tenantContext`, never returned to a client (a request with no tenant logs `tenantId: null`, honestly). Governs `auth.logout` and `auth.logout_all` too — both genuinely have no tenant. `organization.switch` (slice 6) does NOT use it: unlike those three, it resolves a real organization, so its audit event uses that real tenant id, not the sentinel (`DECISION_LOG.md` 2026-08-24, decision 4).

**RISK_REGISTER.md R-003 now ACCEPTED** (was OPEN, "forcing point slice 5"; corrected 2026-08-24 from an interim `RESOLVED`, which read as "the risk is gone" rather than "kept by evaluated decision"): both named alternatives to the `memberships`/`store_memberships` self-access RLS clause were evaluated and rejected — a `SECURITY DEFINER` bootstrap function remains unbuilt and uncoupled; "trust `sessions.active_organization_id` as authoritative" turns out to conflict with ADR-002/ADR-029 (it's UI state, never authority) and doesn't actually remove the bootstrap read. The clause is kept permanently, by evaluated decision — the underlying risk (a policy wider than `tenant_id` alone) is retained, not eliminated.

**RISK_REGISTER.md R-005 (new):** `auth.login` ships with **no rate limiting or lockout** — ADR-029 requires it, `CapabilityDefinition` deliberately has no field for it (nothing enforces it yet), and no module-local substitute was built (a rate limiter is shared platform infrastructure, not a per-capability concern). Must exist before this endpoint is internet-facing.

Argon2id via `@node-rs/argon2` (prebuilt binaries, no node-gyp — this machine is Windows with no Docker) — the first runtime dependency added since `@asteasolutions/zod-to-openapi`. Unknown email, wrong password and a suspended user are indistinguishable in code, message, and timing (one hash-verify call on every non-success path, against a precomputed dummy hash when no real credential exists) — proven by a real-latency sampling test, not just a code-path assertion. `sessions.token_hash` is the only place any form of the token is stored; the cookie is `httpOnly`/`secure`/`SameSite=Lax` per ADR-029, `secure: true` unconditionally (tests copy the cookie by hand, so this breaks nothing locally). `active_organization_id` on a new session is always `null` — auto-selecting a sole membership was rejected because it would need `modules/identity` to depend on `modules/tenant`, a dependency edge that does not exist and wasn't worth creating for a UI convenience ADR-002 already says is never authoritative.

**Task 2 slice 5 now fully done:** `auth.logout` (`POST /api/v1/auth/logout`) and `auth.logout_all` (`POST /api/v1/auth/logout-all`), implemented together as one slice (`DECISION_LOG.md` 2026-08-24 explains why two capabilities, one run — they share a new port, a route prefix, an audit shape, and each other's isolation tests). `SessionGuard` is steps 1–4 for both (there is no organization or store to add); no transaction, same reasoning as `auth.login`; no permission to assert (a user always may end their own session). `auth.logout` ends exactly the caller's current session; `auth.logout_all` ends every ACTIVE session for the caller's user id, **including the one making the request** — the same accepted consequence `membership.role.assign` established for self-role-assignment, not a special case carved out here. A second call to either with the same (now-revoked) cookie returns `AUTHENTICATION_REQUIRED`, not a repeated success: `SessionGuard` rejects before the capability runs, and `auth.logout` is declared not idempotent for exactly this reason; `auth.logout_all` is declared idempotent per `05` §4.1 and genuinely is, in the sense that a *fresh* session calling it again converges to `sessionsRevoked: 0` — not in the sense that the literal same request can be replayed, which the cookie auth model itself rules out.

**Session revocation is one port, not two.** `modules/identity/domain/session-revocation.repository.ts`'s `SessionRevocationRepository` carries both `revokeOne` (end exactly one session — `auth.logout`) and a count-returning `revokeAllForUser` (end every session for a user — `membership.role.assign`, `auth.logout_all`), implemented by one `SessionRevocationRepositoryPg` class, one place that ever writes a revocation to `sessions`. This briefly existed as two interfaces — `SessionRevocationRepository` plus a sibling `SessionTerminationRepository` — specifically so `membership.role.assign`'s existing hand-written fake needed zero changes when `auth.logout`/`auth.logout_all` landed. That read "the fake must pass untouched" as "never edit it," which was stricter than intended; the actual requirement was "don't regress what it proves." Collapsed back into one port 2026-08-24 (`DECISION_LOG.md`) — the fake gained a `revokeOne` stub and a return value, and every assertion in it is unchanged.

**Two corrections landed in the same commit, from independent review:** (a) ADR-035's sentinel safety argument ("`gen_random_uuid()` effectively never produces `PLATFORM_TENANT_ID`") is now backed by a real `CHECK` constraint on `organizations.id`, not just an improbability argument — see ADR-035 decision 5 and `RISK_REGISTER.md`. (b) `audit_events.actor_user_id`'s original migration comment ("nullable for non-user actors") was incomplete, not the stored value — `auth.login`'s unknown-email row (`actor_type='user'`, `actor_user_id=NULL`) was always correct. Corrected via a live `COMMENT ON COLUMN` in a new migration (forward-only rules mean the original file's comment is left as-is). **RISK_REGISTER.md R-003's status corrected from `RESOLVED` to `ACCEPTED`** (see above) — same finding, clearer status word.

**Task 2 slice 6 done — the last of the six: `organization.switch`** (`POST /api/v1/organizations/{organizationId}/switch`). Updates `sessions.active_organization_id`, a display preference (ADR-029 item 5) — nothing else, proved rather than asserted: two integration tests show a request naming a different organization the caller belongs to, or one they don't, behaves identically before and after a switch (ADR-002). Reuses `OrganizationAccessGuard` for membership resolution; its `TenantContext` is used for exactly one thing, naming the audit event's REAL organization tenant — the first user-scoped capability with an actual tenant to attribute to, so it does NOT use ADR-035's platform sentinel the way `auth.login`/`auth.logout`/`auth.logout_all` do. 05 §4.1 rates this `READ`; it is declared `LOW_WRITE` instead and the disagreement is recorded, not silently fixed (`DECISION_LOG.md`). `SessionRepository` (one port, not a new sibling) gained `setActiveOrganization`, exposed through `modules/identity/contracts/` the same way `SessionRevocationRepository` already is — no new dependency edge, `tenant → identity` already existed via `membership.role.assign`. No migration (the column already existed), no permission (every ACTIVE member may switch, regardless of role).

All six of `08_PHASE_1_BRIEF.md` §3's Task 2 slices are now implemented. Exit criteria still **eight of nine** (`PHASE_1_TASK_1_COMPLETION_AND_TASK_2_SCOPE.md` §4); the remaining row is "revoking a membership invalidates active sessions," which needs `membership.revoke` — not one of the six slices, and not scheduled.

**The Phase 1 gate review has run once** — `PHASE_1_GATE_REVIEW_2026-08-24.md`, verdict: gate does not open, solely for the `membership.revoke` gap below; 8 of 9 exit criteria independently confirmed MET. It also raised three findings, all three closed (`DECISION_LOG.md` 2026-08-24, "Closing three findings from the 2026-08-24 gate review" — the dated report itself is left unedited, per this project's convention of never rewriting a dated record): (1) `docker-compose.yml` now pins `postgres:17-alpine` (was `16-alpine`), matching the native install every other document already claimed as the verification basis — seven documents corrected or amended accordingly; (2) the mislabeled test at `store-read.integration.spec.ts:306` retitled — exit criterion 3's application-layer half is met by construction (no reachable code path exists for it), not by a test, and that's the sound state; (3) the error-code contract (`05` §7 vs. each capability's `errorCodes`) is now mechanically enforced by a new conformance rule (`ERROR-CODE-UNDOCUMENTED`/`ERROR-CODE-UNDECLARED`).

**`membership.revoke` is now implemented** — the seventh capability, not one of `08_PHASE_1_BRIEF.md` §3's six-slice list, built specifically to close exit criterion 4 (see `DECISION_LOG.md` 2026-08-24 for why a seventh capability is in scope). `POST /api/v1/organizations/{organizationId}/memberships/{membershipId}/revoke` — an explicit action route, not `DELETE`, since the row survives (status flips to `REVOKED`, never deleted). Owner-only, same tier as `membership.role.assign` and the same reasoning. Refuses with `CONFLICT` when the target is the organization's only remaining ACTIVE member, or its only remaining ACTIVE owner — both are dead ends no later capability could reverse. Revokes every session belonging to the target's user in the same transaction as the status change — the second of `08` §5's three session-invalidation triggers now implemented (role change was the first); password change remains, with no capability yet that could trigger it. Proved end to end, not by mocking: the target's real cookie is used against a real, unrelated capability on the very next request and shown to fail. Found and fixed the same silent-never-updatable Kysely column typing `sessions.revoked_at` once had, this time for `memberships.updated_at`. Found, but deliberately NOT fixed here (`RISK_REGISTER.md` R-006, new): `PermissionCheckRepositoryPg.hasPermission` doesn't independently check a membership's ACTIVE status — currently safe only because the guard that resolves a `membershipId` for a permission check already refuses a revoked one first.

**`08_PHASE_1_BRIEF.md` §6 exit criterion 4 is now believed MET** — proved by `apps/api/membership-revoke.integration.spec.ts`'s "revokes the target's session, so their existing cookie stops authenticating on the very next request (exit criterion 4)" test. Stated here as the implementer's own claim, not as a re-run gate review — the next phase-gate review is independent verification and has not been re-run.

**A second, independent gate review has since run** — `PHASE_1_GATE_REVIEW_2026-08-24-II.md`, re-verifying all nine criteria against HEAD `7d18296` without inheriting the first review's verdicts. Verdict: gate still does not fully open, for a different reason than the first review — 8 of 9 criteria genuinely `MET`, criterion 1 (tenant isolation) graded `PARTIAL` (cross-tenant write/delete empirically correct for all six tenant-owned tables, live-probed, but automated CI proof existed for only `organizations`). It also found a genuine, previously-unknown concurrency race in `membership.revoke`'s last-owner/last-member checks (READ COMMITTED, no lock, no DB constraint backstop) — not gate-blocking on its own, but a real data-integrity defect. **Both are now fixed** (`DECISION_LOG.md` 2026-08-24, "Closing the two remaining findings from the second gate review"): `MembershipRepository.countActive` (a plain count) is replaced by `lockActiveForUpdate` (`SELECT ... FOR UPDATE` on every ACTIVE membership row in the tenant), closing the race — proved by firing two genuine concurrent HTTP requests at a real running app and real PostgreSQL, confirmed to reproduce the pre-fix defect before being kept as the regression test. `apps/api/tenant-isolation-rls.spec.ts` (new) closes the criterion-1 gap by enumerating every RLS-protected table LIVE from `pg_class`/`pg_policy` — never a hand-maintained list — and proving cross-tenant read/write/delete denial for each one it finds, so a Phase 2 migration adding a tenant-owned table with a missing or broken policy fails this file automatically. `RISK_REGISTER.md` R-007 (new, CLOSED) records the race fix. The review's Finding 3 (a stale "PostgreSQL 16" claim in `REPOSITORY_AUDIT_REPORT.md` §3.2, missed by that document's own earlier amendment note) is fixed directly in that file's amendment note. Not yet re-run: a third gate review to confirm criterion 1 is now genuinely `MET`.

380 tests passing (37 files), conformance 0 violations, 20 migrations apply cleanly from empty. 10 capabilities, 10 routes. `npm run lint` and `npm run format:check` both clean; both run in CI.

**CI is live.** `origin/main` on GitHub is a real remote as of 2026-08-23 (this repository had none before); `.github/workflows/conformance.yml` now runs on every push to `main` and actually applies migrations, so the artifact drift checks (`graph -- --check`, `openapi -- --check`) and the live-DB conformance scan all execute for real on `ubuntu-latest`/Node 24, not merely on this Windows machine. First fully green run: 2026-08-24 ("graph: a layer with no files is not a layer", CI run `32666989546`) — see `RISK_REGISTER.md` R-001 (now CLOSED) and `DECISION_LOG.md` 2026-08-24 for the two real defects the two red runs before it caught (a missing migration step; platform-dependent sorting) plus one defect the diagnosability work it forced then surfaced on its own (an empty-directory artifact in `PROJECT_GRAPH.md`).

**The Phase 1 gate is now OPEN** (`PHASE_1_GATE_OPEN_2026-08-27.md`). `PHASE_1_GATE_CONFIRMATION_2026-08-26.md` had found all nine exit criteria MET but withheld the gate because CI never executed on `fd13c1e` (the repository had briefly gone private; cause now IDENTIFIED, not merely correlated — a $0, stop-usage Actions budget on the personal account blocks all private-repo Actions scheduling outright, indefinitely, and currently cannot be edited or removed through the UI; remediation is BLOCKED externally pending GitHub Support, a payment method, or a move to org billing — see `DECISION_LOG.md` 2026-08-27/28). CI run `33014271741` on `fbc7ba4` (code tree byte-identical to `fd13c1e`; diff is `.gitignore` + a dated report only) is fully green, satisfying the one outstanding condition. `RISK_REGISTER.md` R-008 (OPEN, intermittent) records an unrelated same-code CI failure found while checking this, on the `membership.revoke` two-owner concurrency test — root cause UNDETERMINED. R-007 stays CLOSED on the locking mechanism itself, not on any observed HTTP status (corrected 2026-08-28, see R-008). **R-009 (new, OPEN, structural):** all ten controllers call the audit write outside the try/catch wrapping the domain transaction (the golden path's own shape) — verified by reading all ten, nine are protected by a unique constraint or guard chain, `auth.login` genuinely isn't (a retry can orphan an extra session row). R-003/R-005/R-006, the coarse revoke lock, the RLS same-row-probe nuance, and the missing ADR-009 idempotency service all carry forward unresolved.

**`PHASE_1_DEBT_CLOSURE.md` D-1 is now CLOSED** — the calendar/timezone half of `06_IMPLEMENTATION_PLAN.md` Phase 1 item 5 (the clock-abstraction half shipped in Task 1 as `platform/clock.ts`). New module `modules/calendar/domain/business-calendar.ts`: pure, stateless Gregorian calendar-arithmetic functions over explicit instants + IANA time zone strings (`addCalendarMonths`/`addCalendarYears` with end-of-month/leap-day clamping, DST-safe `addCalendarDays`, `startOfDay`/`dayBoundary` for ADR-031's half-open convention, `compareInstants`/`isWithinHalfOpenInterval`) — zero new runtime dependencies (`Intl.DateTimeFormat` confirmed sufficient; Node has no global `Temporal` yet). Five decisions logged in `DECISION_LOG.md` 2026-08-29: module placement (a module, not `platform/`, matching `modules/money`'s own precedent); the calendar-system split — Gregorian is authoritative for all billing/boundary arithmetic, **Jalali is a presentation-only concern for every human-facing surface** (merchant admin and each merchant's own storefront), decided by the user and recorded normatively but **not built** (no UI exists yet to consume it) — with the invariant that no calendar conversion may ever appear inside a billing/boundary computation, and a candidate future conformance rule to enforce it mechanically; the zero-dependency choice; and the DST gap/overlap disambiguation policy ADR-031 leaves silent (Temporal's `'compatible'` default, adopted). A real algorithm bug — gap-resolution silently depending on iteration-count parity rather than being deterministic — was caught by this slice's own tests before shipping; see `DECISION_LOG.md` for the fix. No table, no migration, no subscription/billing logic, no Jalali formatter. `PROJECT_GRAPH.md` regenerated (7 modules now). **Three same-day corrections** (`DECISION_LOG.md` 2026-08-29): the disambiguation policy is confirmed to exactly match ECMAScript Temporal's `'compatible'` default (checked against real Temporal docs, not assumed — a future migration to native `Temporal` is therefore behavior-preserving); `startOfDay`'s doc comment overclaimed "always midnight" (a zone whose DST transition falls exactly at midnight, confirmed real — America/Santiago — correctly hits the gap policy instead; now tested, +1 test); a fast-path skipping a validity check was assessed and proven safe for this file's own call sites, not defensively guarded.

**`PHASE_1_DEBT_CLOSURE.md` D-2 is PARTIALLY CLOSED, 2026-08-30** — deliberately, two separate decisions (`DECISION_LOG.md` 2026-08-30). **Done:** a `redis` service in `docker-compose.yml` (`redis:7-alpine`, host port `6380`, matching `postgres`'s `5433`-not-default convention), wired into the same CI compose-up step (renamed "Start/Stop PostgreSQL and Redis"), a commented-out `REDIS_URL` in `.env.example`. **Deliberately NOT done:** the BullMQ/Redis client dependency — nothing calls it yet, and a queue's worker topology/retry/dead-letter design needs a first real job to design against, the same "declare what nothing enforces" reasoning `CapabilityDefinition` already applies to `entitlements`/`quota`/`approval`. Deferred with a hard, checkable trigger this time (unlike the 2026-08-22 deferral D-2 exists to fix): add `bullmq` when `06_IMPLEMENTATION_PLAN.md` **Phase 2 item 14** (renewal lifecycle jobs) is picked up. This machine has no Docker and no native Redis — an operational item for whoever eventually needs local Redis, not solved here.

**`PHASE_1_DEBT_CLOSURE.md` D-3 is now CLOSED, 2026-08-31, in two separate commits** (`DECISION_LOG.md`'s two 2026-08-31 entries). **Commit 1 — pure extraction, zero behavior change:** all ten controllers' identical outcome-tracking + audit-write + rethrow tail is now one function, `runCapabilityAttempt` (`modules/capability/interfaces/capability-attempt.ts`, exported via `modules/capability/contracts/`). Nothing that genuinely varies by capability moved into it — guard chains, whether/how a transaction opens (`auth.login`/`auth.logout`/`auth.logout_all` open none), and each capability's own `AuditEvent` fields all stay exactly where they were, as parameters (`work`, `buildEvent`) rather than branches; see `DECISION_LOG.md` for the full ten-way variance read that preceded extracting anything. Proven zero-behavior-change the only way that is actually provable: all 378 existing tests passed with zero test files edited. `AGENTS.md` §2, `.claude/skills/new-slice/SKILL.md`, and `store.controller.ts`'s own doc comment all updated to teach the shared helper instead of the hand-rolled skeleton — explicitly **not** Phase 5's capability registry/policy pipeline, a boundary recorded in all three places. A conformance rule forbidding hand-rolling the skeleton again was assessed and deliberately not built (no evidence yet anyone would). **Commit 2 — R-009 fixed, in this one now-shared place:** the audit write is wrapped in its own `try/catch`; its failure is logged and swallowed, never allowed to override the already-decided domain outcome (framed as an extension of ADR-034's own accepted residual cost, not a new rule). Proven by two new tests independently confirmed to fail against commit 1's code and pass against commit 2's, checked directly (not merely asserted) — 380 tests total. `RISK_REGISTER.md` R-009 closed. One conformance false positive (a doc comment's own prose literally matched `DB-ACCESS-TRANSACTION-BYPASSES-HELPER`'s regex) fixed by rewording, not by adding an exception. `db:migrate` still 20, `openapi.json` unchanged throughout — no route, contract, or migration touched by either commit.

**`PHASE_1_DEBT_CLOSURE.md` D-4 is now CLOSED, 2026-09-01, in three commits** (`DECISION_LOG.md` 2026-09-01). Tooling decided and logged: ESLint + `typescript-eslint` (type-aware, `recommendedTypeChecked`) + Prettier over Biome, argued from this codebase's own `strict`/`noUncheckedIndexedAccess` posture and its own async-typing bug history, not preference. **Commit 1** — pure Prettier setup + one whole-tree reformat, nothing else, verified behavior-neutral (typecheck clean, all 380 tests, conformance's identical 0 violations before/after — checked specifically since its rules regex-match source text — `db:migrate` 20, `openapi.json`/`PROJECT_GRAPH.md` unmoved); `.git-blame-ignore-revs` added and verified in its own follow-up commit. `.prettierignore` excludes generated artifacts, conformance fixtures, and **all markdown** (dated records must never be rewritten, even cosmetically). **Commit 2** — ESLint + CI steps for `lint` and `format:check`. Every widely-firing rule read and decided individually, not blanket-suppressed: `no-unsafe-*`/`require-await` (302 combined findings, effectively 100% in `*.spec.ts` from supertest's untyped bodies and test fakes not needing to await) turned off for test files only — fixing either would mean touching nearly every test file, the mass refactor this slice was warned against, for findings that fail the test itself if wrong. `preserve-caught-error` (18 findings) was fixed, not disabled — genuinely valuable, mechanically safe (`{ cause: err }`). Two single-instance findings got named inline disables; `no-unused-vars` was configured, not disabled, to respect an existing `_`-prefix convention. `npm run lint`/`format:check` both clean; typecheck/tests/conformance/migrations/graph/openapi all unchanged from before this slice.

**`PHASE_1_DEBT_CLOSURE.md` D-6 is now CLOSED, 2026-09-02 — R-008 investigated properly, on its honest outcome, not fixed.** R-008's test failed a third time (three occurrences total now, no longer "reproduced exactly once"). Full investigation in `DECISION_LOG.md` 2026-09-02: all three occurrences share an identical assertion failure, confirmed by reproducing the exact message locally to mean neither request's status was genuinely 200 — yet each occurrence's own server log shows a clean 200 for that test's own org, an unresolved contradiction. **165 local reproduction attempts (150 isolated + 15 full-suite runs), zero anomalies** — could not reproduce. The deadlock hypothesis was tested directly, not inferred: zero `pg_stat_database.deadlocks` increase across 150 attempts, no 5xx status in any real occurrence — **REFUTED, not merely unconfirmed.** No fix applied (nothing to measure a fix against, and the evidence doesn't point at lock ordering); `ORDER BY id` stays a named, unapplied candidate. `RISK_REGISTER.md` R-008 re-rated Low → Medium likelihood, impact reassessed, **stays OPEN** — D-6 closes because the required investigation happened and is honestly recorded, not because the flake is fixed. Test diagnostics improved regardless (`apps/api/membership-revoke.integration.spec.ts`): final `memberships` state now captured unconditionally before any assertion that could throw, with a full diagnostic dump on any anomaly — no change to `membership.revoke`'s business rules.

**A real production build path exists, and `.env` actually works.** `npm run build` (`tsc -p tsconfig.build.json`) emits `dist/apps/api/main.js`; `npm start` runs it under plain `node`. Proven booted, not just compiled: a real `npm ci --omit=dev` install in a scratch directory, then a real HTTP request against the compiled output. Node 24's `--env-file-if-exists=.env` is wired into every script that needs config (`start:dev`, `start`, `db:migrate`, `conformance`, `test`) — copying `.env.example` to `.env` is now sufficient to run everything on this machine with zero manual `export`. `tools/build/check-dist-deps.ts` (`npm run check:dist-deps`, wired into CI) fails the build if `dist/` ever imports a devDependency again — the defect that motivated it (`apps/api/create-app.ts` transitively importing `@nestjs/testing` via `createTestApp`) is fixed, `createTestApp` now lives in `apps/api/test-support/create-test-app.ts`.

**HTTP-level hardening landed:** security response headers (`apps/api/security-headers.middleware.ts`), CORS explicitly denied by default (`app.enableCors({ origin: false })`, verified against the `cors` package's actual source, not its README), `X-Powered-By` disabled at the Express instance rather than merely overridden. `GET /health` exists — liveness plus a real `select 1` readiness check against `APP_DB` — deliberately not a capability (no tenant, no permission, no audit event), which required deciding how a non-capability route coexists with the graph/openapi tooling (it shows up as a route, never as a capability).

**`RISK_REGISTER.md` R-005 (`auth.login` had no rate limiting) is PARTIALLY CLOSED:** `platform/rate-limit/` (a `RateLimitPolicy` type, a `RateLimitStore` port, an `InProcessRateLimitStore` using the injected `Clock`) is shared platform infrastructure, applied to `auth.login` only, keyed per-identifier (5/15min) and per-IP (30/15min), checked before the Argon2 verify. Proven not to reopen the enumeration channel `LoginService` itself closes (a dedicated test drives an unknown email and a real wrong-password account through an identical sequence and confirms both throttle identically). Two real production-only defects found and fixed afterward: the window map had no eviction (fixed with a full sweep on write; the sweep's own O(n)-per-write cost under a live burst is now `RISK_REGISTER.md` R-013, explicitly UNMEASURED, with a two-generation-map alternative named); and the controller keyed on a bare `request.ip`, which collapses to a proxy's address behind any real load balancer — `platform/http/client-ip.ts` is now the one named place client IP is derived, `TRUST_PROXY` (`platform/config.ts`, safe default `false`) is now `RISK_REGISTER.md` R-012, both failure directions (unset-behind-a-proxy vs. blindly-trusted) named explicitly so neither gets "fixed" into the other. Still open in both rows: the store is per-process (no Redis-backed replacement yet), and `TRUST_PROXY` still needs a real value the day a real proxy exists.

**`RISK_REGISTER.md` R-010 (a sustained `AUDIT_DB` outage is silent) has its detectability half closed, its alerting half explicitly still open.** `capability-attempt.ts`'s audit-write catch block now emits a stable, structured, greppable event (`AUDIT_WRITE_FAILED_EVENT`) and an in-process counter (`getAuditWriteFailureCount()`) — deliberately not wired into `GET /health` (public, unauthenticated; audit health is also orthogonal to whether the instance should keep serving traffic). Alongside this, the `AUDIT_DB` naming was checked against ADR-034 directly (not assumed): `createAuditDb()` already satisfies ADR-034's actual requirement, a separate connection pool — it was never a violation — but it read as if a separate DATABASE existed too, which it did not. `AUDIT_DATABASE_URL` (`platform/config.ts`, defaults to `DATABASE_URL`) makes a genuinely separate audit database a configuration change instead of a code change, proven byte-identical today with it unset.

**`RISK_REGISTER.md` R-008's candidate mitigation (2) is done: a PostgreSQL deadlock (40P01) or serialization failure (40001) now reaches the client as `CONCURRENCY_CONFLICT`/409, not `INTERNAL_ERROR`/500** — `platform/db/concurrency-error.ts` (mirroring `constraint-violation.ts`'s own duck-typed shape) consumed from `HttpExceptionFilter`'s catch-all branch, a new `05_API_CAPABILITY_CONTRACTS.md` §7 code (explicitly documented as RETRYABLE, unlike `CONFLICT`), added to the six capabilities that open a real multi-statement write transaction (not all ten — `auth.*` opens no transaction, `store.read` takes no locks). Proven against a genuinely induced real Postgres deadlock (two manually-controlled Kysely transactions deliberately cross-waiting), not a mock. R-008 itself was not touched — `lockActiveForUpdate` still has no `ORDER BY`, still UNDETERMINED, still OPEN; this was defense-in-depth for Phase 2's added concurrent writers, independent of R-008's own root cause.

**Five closing decisions Phase 1 never wrote now exist, before Phase 2 opens** (`decisions/2026-08.md`): `PHASE_1_DEBT_CLOSURE.md` D-5 (`outbox_events`/`identity_providers`) is CLOSED on the decision, not the tables — both stay absent, each with a real trigger (the first capability that actually needs one) rather than the lapsed "when that slice starts" framing. ADR-009's idempotency service is confirmed to already sit correctly at Phase 2 item 3, not Phase 1 debt — named explicitly: no capability whose retry correctness depends on it (the future payment/reconciliation work) may ship ahead of it. Cross-module referential integrity's trade-off (no FKs across module boundaries, nothing detects an orphan) is recorded with a trigger (the first hard-delete capability for a cross-module-referenced row) rather than built speculatively. The CI contingency (self-hosted runner, if the GitHub Support ticket for the $0 Actions budget stays unresolved) has a named decision point (before the repo ever goes private). `RISK_REGISTER.md` R-006 was re-confirmed safe by reading every current caller fresh, not by inheriting the prior assessment — all five of `hasPermission`'s current call sites pass only the caller's own guard-resolved membership id.

**An external architecture review was recorded, verified finding by finding, and migrated into the register — no code changed in either pass.** `EXTERNAL_ARCHITECTURE_REVIEW_2026-08-28.md` tested sixteen handed-over findings (F-1 … F-16) against the actual files rather than accepting them, and a following session migrated the confirmed ones into `RISK_REGISTER.md` as **R-020 … R-031** (twelve rows; the register now holds 31). Neither pass wrote a line of TypeScript or SQL, changed a config file, touched an ADR, or added a test — the whole of both is documentation.

**The most useful outcome is that three handed-over claims did not survive contact with the code, and the corrections are carried in the rows rather than quietly dropped.** (1) **F-7** asserted all six capabilities that `05` §4.1 marks `Idempotency: yes` declare `idempotent: false`; `auth.logout_all` in fact declares `true`, with a doc comment explaining it is naturally idempotent (revoking every ACTIVE session converges) — the divergence is five of six, not six, and `grep -c "idempot" openapi.json` returns 0, so the *generated* artifact clients consume was never dishonest, only `05` §4.1 is optimistic. (2) **F-14** claimed data-driven RBAC rests on one test case; the named file tests `ROLE_KEYS`↔`roles`-table agreement, not permissions, and permission enforcement is separately covered by 34 `FORBIDDEN` assertions across six integration suites — refuted, with only an unrecorded six-vs-three role-count divergence in `00_PLATFORM_OVERVIEW.md` surviving as R-028. (3) **F-15** claimed no secrets rule exists; ADR-023 item 8 already mandates that store-scoped credentials are "encrypted at rest, never returned by any read API, and never logged," so the premise was refuted while the forward gap stands — nothing implements it, and Phase 2 item 11 stores the first PSP credential. R-029 is the closest any of these comes to blocking: shipping item 11 unresolved would violate an accepted ADR, not merely enter undefined space. Smaller citation corrections landed in R-025 (`03` §9 *does* name object storage as a day-one seam) and R-027 (`schema-live.ts` *does* catch the dangerous class of live drift, by enumerating real tables).

**Four findings did not become new rows, deliberately.** F-3 (a CI-parallelism-over-unbounded-pools hypothesis for R-008's intermittent failure) was appended to **R-008** as an explicit HYPOTHESIS, UNTESTED, carrying its own counter-argument — D-6's refutation of the deadlock hypothesis stands untouched and this must not be read as a diagnosis. F-5 (two inconsistent logging paths, no redaction) was appended to **R-010** without restating the alerting gap that row already owns. F-7 was appended to **R-017**. F-10 (`store.update` contracted, built nowhere) became nothing — already recorded as G-10, whose reasoning still holds. **F-1 was fixed rather than tracked:** `CLAUDE.md`'s roll-call had gone stale (it claimed 13 rows against a register holding 19) and is re-synced here, verified by extracting every row's Status cell mechanically rather than by trusting the edit. Its *structural* cause is now **R-031**: this repository mechanically guards generated artifacts (`graph --check`, `openapi --check`) and architecture (ADR-030) but nothing guards the governance documents every session reads first — demonstrated, not hypothesised, since `PHASE_2_ENTRY_REVIEW_2026-08-28.md` §4 verified that exact line as accurate hours before it went stale. The register's Phase 0-era preamble ("not populated yet — no feature code exists") was also replaced with an accurate one defining the status vocabulary and the no-rewrite rule; no existing row was reworded.

**`PHASE_2_BRIEF.md` exists and Phase 2 is open.** The draft's fourteen open decisions (D2-1 … D2-14) were answered by the maintainer on 2026-08-28, folded into the sections where they operate, and `AGENTS.md` §1 item 2 now points at `PHASE_2_BRIEF.md` instead of `08_PHASE_1_BRIEF.md` — which stays referenced as the closed phase's record, because its §5 RLS exemption list is still the only explanation of the existing schema's tenancy. `RISK_REGISTER.md` **R-014 is CLOSED** (both halves: the brief exists, and §3(a) maps all 15 of `05` §4.2's capabilities to their owning `06` items with every assignment marked EXPLICIT or INFERRED). No code changed; this whole sequence was documentation.

**Three of the fourteen answers were amended by the maintainer rather than accepted as recommended, and the amendments are the more useful record.** **D2-2** (credentials): deferring the encryption *mechanism* was granted, deferring the *column shape* was refused — item 10's `billing_provider_configs` must store a secret reference or an envelope-shaped column and must never hold a plaintext secret, including in a stub or fixture, because once that table exists an undecided column shape is a schema commitment and migrations are forward-only. **D2-8** (roles): the conclusion (defer the six-vs-three catalog question to Phase 3) stood, but the reasoning was rejected — "no Phase 2 capability needs the distinction" is false, since every billing capability needs a `role_permissions` seed row, which *is* a catalog decision; the correct ground, now a rule, is that **every Phase 2 billing permission is granted to `owner` and `admin` only**, which is right under both the implemented three-role catalog and `00`'s six-role one and so forecloses neither. **D2-12** (`subscription.renew`): routed to item 14, not item 12 as recommended — ADR-024 item 5's extension arithmetic is a subscription-domain invariant and must not be implemented inside the payment slice.

**The exhaustive crosswalk D2-5 required found two gaps neither ADR covers, now `RISK_REGISTER.md` R-033.** Enumerating all 8×4 = 32 combinations of ADR-024's subscription statuses against ADR-020's tenant-data states gave 20 possible and 12 impossible with a stated reason each — and surfaced `ACTIVE`×`SUSPENDED` and `ACTIVE`×`OFFBOARDED` as genuinely undefined (does billing continue during tenant suspension? must a live paid subscription be cancelled before a deletion request?), plus a third gap: ADR-024 item 6's "offboarding follows ADR-020" has no owning item in `06`, so nothing schedules who moves a tenant into `OFFBOARDED` or when. All three are unreachable by construction during Phase 2 — no Phase 2 capability transitions a tenant between ADR-020 states, and the brief forbids adding one — which is why this is a recorded gap rather than a blocker. **R-032** was also opened, for D2-11: `00_PLATFORM_OVERVIEW.md` §4.2 promises plan tiers "configurable without a code deployment," no capability in `05` creates or edits a plan, no item in `06` delivers one, and Phase 2 administers plans by seed migration — a product promise with no owning phase item, recorded as NOT MET rather than quietly ignored.

**The first implementable slice is `06` item 1 — "plan and plan version", delivering `plan.list`.** It is the Phase 2 reference slice and carries a hand-review stop, because it introduces three patterns every later slice copies: a collection read and therefore pagination (keyset/cursor, opaque, one style platform-wide), platform-global reference data with no `tenant_id` and no RLS policy — inverting Phase 1's universal assumption — and a versioned immutable aggregate. A second review stop is set at item 4 (`plan.subscribe`), the first tenant-scoped write combining `Money`, the state machine and idempotency, since one reference slice cannot cover both shapes. Table ceiling: **27 tables**, with `subscription_items` and both AI-credit tables explicitly out.

**Five ADRs were added and two amended, paying the debts `PHASE_2_BRIEF.md` §7 recorded as owed to the ADR index.** The index goes 38 → **43 ADRs (33 accepted)**; `PROJECT_GRAPH.md` was regenerated (`npm run graph`, never by hand) and `npm run graph -- --check` passes, which matters because the graph carries a mechanically-parsed ADR register and CI would otherwise go red on the drift. Three of the five are `ACCEPTED (new)` and record decisions that were already made but living only in `PHASE_2_BRIEF.md` §5 — the wrong home, since a platform-wide contract expires with the phase that houses it: **ADR-036** (collection pagination — keyset/cursor, opaque, one style platform-wide), **ADR-037** (credential storage: a `secret_ref` to a secret held outside the database, never an envelope column and never a plaintext secret, because migrations are forward-only so item 10's column shape is a commitment the moment it merges), and **ADR-038** (idempotency composition — `withIdempotentCapability` inside `runCapabilityAttempt`, never a branch in it). Two are `OPEN`, using this index's own word for a decision not yet made: **ADR-039** (pool sizing and query timeouts) and **ADR-040** (observability boundary), each with options and a recommendation for the maintainer to rule on.

**Writing ADR-006's amendment corrected the decision that requested it, which is the most useful outcome of the session.** D2-6 had descoped AI credit accounting from Phase 2 on the reading that ADR-006's `Blocks: Phase 2` was simply stale. Checking `06` item by item shows ADR-006 governs *two* things: a usage/credit-ledger concurrency requirement — "ledgers are authoritative and must be protected against concurrent modification… `SELECT FOR UPDATE`, database constraints, and idempotency" — which applies directly to **Phase 2 item 9, "usage ledger," and therefore genuinely does block Phase 2**; and an AI-credit reservation lifecycle, which has no consumer until AI exists. The designation was therefore **split, not moved** (`Phase 2 (usage-ledger half) · Phase 6+ (AI-credit half)`), and ADR-006 gained two verification items so the surviving half cannot be quietly read as deferred. ADR-010's amendment restates its numeric targets as unverified assumptions measurable at `06` Phase 4 item 9, changing their epistemic status without touching a number. The `entitlement.resolve` p95 budget was assessed for ADR-010 and deliberately left owed — checking showed ADR-010 *already* bounds `entitlement.resolve` as a request via "Admin API p95 under 500 ms"; what is unbounded is resolution as an inner pipeline step, a different kind of number that would have to be invented to put it there.

Seven register rows (R-010, R-016, R-017, R-019, R-020, R-024, R-029) gained dated addenda naming the ADR that now owns each decision and what remains. **None was closed** — an ADR is a decision, not an implementation, and R-003's precedent on status words applies.

**Two non-normative documents now exist, and two stale documents were brought current** (2026-09-01). `TECHNOLOGY_RADAR.md` collects every emerging-technology judgment this repository had scattered across register rows and ADR context paragraphs — 21 entries across Adopt/Trial/Assess/Hold, each anchored to a specific register row, ADR or phase item, with entries that could not be anchored cut rather than included. `COMPETITIVE_POSITION.md` states the moat first, the deliberate omissions second, and the genuine gaps third — deliberately in that order, so §3 does not read as a backlog inviting someone to "fix" a decision ADR-021 or ADR-023 already made. **Both are explicitly outside `AGENTS.md` §1's read order and say so**: a radar that acquires authority becomes a second, competing decision record.

**The epistemic split in those two documents is the part worth knowing.** Claims about Nexora are verified against the repository with paths cited; claims about PostgREST, Hasura, Supabase, Strapi, KeystoneJS, OpenFGA and the rest are marked *understood at 2026-09-01, not verified against upstream in this pass* — web search was available and used, but search results are secondary sources. That asymmetry is stated in both documents' Method sections rather than papered over, because nothing in this repository can contradict a wrong claim about an external project. One claimed strength did **not** survive checking and is recorded as qualified rather than dropped: `05` §1's "REST, Admin UI, Storefront, AI, MCP, Automation and Plugins converge on the same Application Service" is a real design property with real machine-readable metadata behind it, but **only REST exists today** — the convergence is unproven, not proven.

**Corrections landed.** `PROVIDER_MATRIX.md` now carries a dated correction block: its "no earlier than Phase 3/4" claim cited `03` §9, which contains no phase numbers at all, and contradicted `06` Phase 2 items 10–12; decision D2-3's split ruling replaces it, with ADR-023/ADR-037 as authority. **R-015 does not close** — the file is fixed, but no provider has been selected, which is what the row actually tracks. `07_ARCHITECTURE_GAP_REPORT.md` gained a §8 currency amendment (**amended, not rewritten** — §1, §2 and §6 are still accurate and §2's 28-row record of what the 2.0 pack fixed is unreproducible provenance): of its eight by-design open gaps, two are now closed with evidence (the repository audit, and generated OpenAPI artifacts), five are unchanged, and one is superseded by ADR-010's amendment. §4's readiness checklist is fully met with one item superseded by `PHASE_2_BRIEF.md` replacing `08` as authority #2; §5 and §7 are complete and marked historical; §6's release blockers are unchanged and still binding.

**Two previously-unanchored gaps became rows.** **R-034**: no frontend of any kind exists and no `06` phase item delivers one, while `00` and `05` §1 both assume an Admin UI — verified by grep across all seven phases. **R-035**: no development seed data and no seed script; every `INSERT INTO` in the repository seeds reference data inside a migration, and Phase 2 sharpens this because D2-11 administers plans by seed migration. The register is now **35 rows, 28 OPEN**; nothing closed this session.

**The documentation programme closed 2026-09-01 with three ADRs and two repairs.** **ADR-041** (ledger and audit table growth, `OPEN`) states precisely what ADR-020 does and does not commit the platform to — rules 4–5 *exclude* append-only financial and audit rows from purge, so deletion is not an option and these tables only ever grow, a consequence no document previously owned — lays out four options with a recommendation, and names the timing problem R-030 understated: the four Phase 2 ledger tables do not exist yet, which is the cheapest moment they will ever have. It also found that partitioning would interact with `schema-live.ts`, which enumerates every `BASE TABLE` and independently demands `tenant_id` + RLS + `FORCE` + a policy on each, and it records four PostgreSQL semantics questions as **owed empirical verification rather than asserting them**, because they cannot be settled without running the database. **ADR-042** (`ACCEPTED`) rules the question R-026 had recorded as genuine ambiguity: `message` is developer-facing and never shown to an end user, `code` is the localization key, `details` carries the parameters — decided by a precedent already sitting in `05` §7 (`QUOTA_EXCEEDED` must carry structured `resource`/`current`/`limit`/`resolution`; "a bare limit error is not an acceptable contract"), which is only coherent if a consumer composes its own text. **No localization work is owed in Phase 2, as a ruling rather than a deferral.** **ADR-043** (`ACCEPTED`) found that subset, superset and exactly-equal assertions against `05` §5 all reject *correct current code*, and asserts the declared difference instead — catching the failure review reliably misses, a `05` §5 field quietly dropped from the deferred list, which erases the evidence it is still owed while looking like progress.

**Two repairs.** A citation defect in this programme's own prior session: `PROVIDER_MATRIX.md` attributed the provider-timing contradiction to `EXTERNAL_ARCHITECTURE_REVIEW_2026-08-28.md` F-3, which is the untested pool-saturation hypothesis for R-008 and has nothing to do with providers — the finding was G-3, and the external review has no provider-timing finding at all. Fixed in place, and **all 20 `F-n`/`G-n` citations the programme produced were audited; that one was the only error.** Separately, `RISK_REGISTER.md`'s R-008 row spanned four physical lines, which terminates the Markdown table in every CommonMark renderer — so the register rendered broken from R-008 onward, and it is what made an earlier session's `awk` extraction silently drop that row. Traced to `b629cf1` (2026-08-27), **pre-existing, a day before this programme began.** Reflowed onto one line and proven byte-identical after whitespace normalisation; no other row had the defect. A dated collision note was added to `decisions/2026-09.md` where the real 2026-09-01 entry meets the fictional one left by the old calendar drift, without editing either.

**What the seven sessions collectively produced, and what they deliberately did not.** Between 2026-08-28 and 2026-09-01: `PHASE_2_BRIEF.md` (authority #2, 27 tables, 26 exit criteria, fourteen answered decisions); eight new ADRs (036–043) and two amendments (006, 010); the register grown from 13 rows to **35**, of which 28 are OPEN; `TECHNOLOGY_RADAR.md` and `COMPETITIVE_POSITION.md`; corrections to `PROVIDER_MATRIX.md` and a currency amendment to `07_ARCHITECTURE_GAP_REPORT.md`. **Not one line of TypeScript, SQL, configuration or test changed in any of the seven.** The only generated artifacts touched were `PROJECT_GRAPH.md` and `project-graph.json`, and only by running `npm run graph`. The test suite, the schema, the migrations and every runtime behaviour are exactly what they were on 2026-08-28 — every claim in this programme is about what the repository already was, not about anything it was made to do.

**CI is red on `main`, stated plainly.** The documentation programme's push (`e04f3c1..55a5abf`) triggered CI run `33555601029`, which failed. Every step that could plausibly be affected by a documentation-only change passed — type check, build, lint, format check, conformance scan, `graph --check`, `openapi --check`. The failure is `R-008`, the pre-existing intermittent concurrency-test failure, occurring for the fourth time. This is **not a regression from this push** — `55a5abf` changed zero lines of code — and is recorded rather than silently re-run.

**Occurrence 4 turned out to be genuinely informative, and split R-008 into two distinct, separately-tracked threads.** D-6's diagnostic dump — added after occurrences 1–3 left no trail — fired for the first time and showed a clean `200`/`403` pair with the correct final database state (`ACTIVE`/`REVOKED`, exactly one owner). Read against the guard chain (`SessionGuard`, then `OrganizationAccessGuard`, racing against the winning request's one atomic commit), this is now a **fully explained** timing window — the *same* family of race the test's own doc comment already documents for its 401-vs-409 pair, just caught at a third checkpoint nobody had catalogued. It does **not** explain occurrences 1–3, whose central fact — `successes` reading empty despite a logged `200` — remains genuinely UNDETERMINED; occurrence 4's `successes` array was correctly populated. `R-008` therefore now carries both threads explicitly, with a textual safeguard against closing one and having it read as closing the other. **F-3** (the untested connection-pool hypothesis) was proposed for the unexplained thread only, so occurrence 4 — clean responses, no stall anywhere — is not evidence about it either way; its prior is unchanged.

**The explained half revealed a real, separate gap: a membership revoked mid-flight has no defined response code.** Depending on exactly when the winning request's commit lands relative to the losing request's own two guard checks, a client gets `401`, `403`, or `409` for what is, from the platform's perspective, one condition — and `05_API_CAPABILITY_CONTRACTS.md` never says which is correct, or that this is deliberately timing-dependent. A related oddity found in the same pass: `05` §7 already declares a `SESSION_INVALIDATED` code, mapped to 401, that is never actually thrown anywhere in the codebase. Opened as **R-036**, cross-referenced to R-008 rather than merged into it, with an explicit trap recorded: widening the test's classification to accept `403` would silently decide the contract question that row exists to keep open.

**Recommendation: re-run CI once this record is committed, with the result's actual meaning stated in advance.** A green rerun would confirm what is already known — the failure is intermittent, not permanent — and would prove nothing about R-008's still-UNDETERMINED thread, since that thread has never been reproduced on demand in 165 local attempts either. A green rerun is expected and is not evidence of anything fixed.

Update this section when that changes.
